\documentclass[12pt,fleqn]{article}\usepackage{../../common}
\begin{document}
Ders 4

Dýþbükeylik (Convexity) ve Koniler (Cones) 

Bir lineer vektör uzayýndaki $K$ kümesi diþbukeydir (convex), eðer 
$x_1,x_2 \in K$ için $\alpha x_1 + (1-\alpha)x_2$, $0 \le \alpha \le 1$ formundaki
tüm noktalar da $K$ içinde ise.

Matematiksel olarak $\alpha,1-\alpha$ ile yapýlmaya uðraþýlan $x_1,x_2$
``arasýndaki'' bir noktayý temsil etmek. Eðer $0 \le \alpha \le 1$ ise,
$x_1,x_2$'yi sýrasýyla $\alpha,1-\alpha$ ile çarpýp sonuçlarý toplamak
``biraz $x_1$'den, biraz $x_2$'den almak'' anlamýna geliyor, bu da taným
itibariyle her zaman $x_1,x_2$ arasýnda bir yerde olmaktýr. $\alpha$ 0 ile
1 arasýndadýr, yani bir nevi yüzde hesabý yapýlýyor, 0.2 oradan, 0.8
þuradan alýnýyor. Hesabýn bir tür yüzde hesabý olmasý sebebiyle iki nokta
arasýnda kalýnmasý garanti edilmiþ oluyor. 

\includegraphics[height=3cm]{4_1.png}

Ve eðer bu ``arada olmak'' denklemi, kümedeki her noktanýn her diðer
noktayla arasýndaki, yani her $\alpha$ için hesaplanacak noktalar için de
doðru ise, o zaman hep ayný kümedeyi, ``dýþarý çýkmýyoruz'' demektir ve bu
diþbukeyliðin tanýmýdýr. Görsel olarak ta kabaca bunu görmek mümkündür,
diþbukey bir cisimde bir noktadan diðerine düz çizgide giderken hep cisim
içinde kalýrýz.

Teori 

$K.G$ bir vektör uzayýnda dýþbükey olan iki küme olsun. O zaman 

1. $\alpha K = \{x: x = \alpha k, k \in K\}$ her $\alpha$ için dýþbükeydir. 

2. $K+G$ dýþbükeydir. 

Ýspatlamadan bunun daha genel bir hali olan baþka bir teoriye bakalým, onu
ispatlarsak üsttekini de ispatlamýþ olacaðýz. 

Teori 

$\mathscr{C}$, içindeki tüm kümeleri dýþbükey olan rasgele bir buket
olsun. O zaman $\cap_{K \in \mathscr{C}}K$ ayný þekilde diþbukeydir. 

Ispat

Diyelim ki $C = \cap_{K \in \mathscr{C}}K$. Eðer $C$ boþ ise, teori hemen
ispatlamýþtýr. Diðer þartlar için, farzedelim ki $x_1,x_2 \in C$. O zaman
$x_1,x_2 \in K$ demektir, çünkü $C$ bir kesiþim, yani tüm $K \in \mathscr{C}$
içindeki ayný olan öðelerden müteþekkil. Her $K$'nin kendi baþýna dýþbükey
olduðunu bildiðimize göre, o zaman $C$ de dýþbükey demektir. 

$ \square $

Norm Edilmiþ Lineer Uzaylar

Soyut Analiz ve uygulamalarda ilgilenilen vektör uzaylarýnýn 7 önþarttan
daha fazlasýna ihtiyacý vardýr. 7 önþart vektör uzaylarýnýn sadece cebirsel
özelliklerini tanýmlar: toplam, skalar çarpým, ve bunlarýn deðiþik
kombinasyonlarý. Eksik olanlar topolojik olan özelliklerdir, yani açýklýk
(openness), kapalýlýk (closure), yakýnsaklýk (convergence), ve bütünlük
(completeness). Eðer uzayýn içinde uzaklýk ölçümü tanýmlanýr ise, bu
kavramlar kullanýlabilir. 

Taným

Norm edilmiþ bir lineer uzay $X$ adýndaki bir vektör uzayýdýr, ki $X$
içindeki her $x$ öðesini bir reel sayý $||x||$'e eþleyen bir fonksiyon
vardýr, ve $||x||$'e $x$'in norm'ü adý verilir. Norm þu önþartlarý yerine
getirmelidir. 

1. $||x|| > 0$, her $x \in X$ için, ve $||x|| = 0$, sadece ve sadece $x =
\theta$ ise. 

2. $||x+y|| \le ||x|| + ||y||$ her $x,y \in X$ için (üçgensel eþitsizlik) 

3. $||\alpha x|| = |\alpha| \ ||x||$, her skalar $\alpha$ ve her $x \in X$ için. 

Norm kavramý uzaklýk kavramýnýn soyutlaþtýrýlmýþ bir halinden ibaret
aslýnda. Reel analizdeki üçgensel eþitsizliðin karþýlýðý burada da
görülüyor mesela. Neyse, devam edelim, üstteki üçgensel eþitsizlik
kuralýnýn bir uzantýsý / sonucu (lemma) þu:

Teori 

Norm edilmiþ bir lineer uzayda 

$$ ||x|| - ||y|| \le ||x-y|| 
\mlabel{1}
$$

Ýspat

$$ ||x|| - ||y|| = ||x - y + y|| - ||y||$$

Üstte adece $||x||$ içine $-y+y$ ekliyoruz, yani aslýnda hiçbir þey
deðiþtirmedik. Þimdi eþitliðin saðýndaki ilk terimi alýp ona üçgensel
eþitliði uygularsak (norm içindeki $+$ iþareti solu ve saðýndaki gruplarý
ayrý terimler olarak kabul etmemiz gerekir)

$$ ||x - y + y|| - ||y|| \le
||x - y || + ||y|| - ||y|| 
$$

elde ederiz. Biraz daha basitleþtirince

$$ ||x|| - ||y||  \le ||x - y ||  $$

$\square$

Uygun bir norm bulunabilirse, daha önce gösterdiðimiz vektör uzayý
örneklerinin çoðunluðu norm edilebilen uzaya dönüþtürülebilir.

Örnek 1

$C[a,b]$ adý verilen norm edilmiþ uzay, $[a,b]$ reel aralýðý, artý norm

$$ ||x|| = \max_{a \le t \le b} |x(t)| $$

tanýmýndan oluþur. Hatýrlarsak bu uzay daha önce bir vektör uzayý olarak
gösterilmiþti. Norm $[a,b]$ aralýðýna bakýyor, her $x(t)$ için mutlak deðeri
(absolute value) en yüksek olan deðeri alýp onu norm deðeri ilan
ediyor. Fonksiyon bir parabol ise, parabolun tepe noktasý o fonksiyon için
norm kabul edilecek. 

Þimdi teklif edilen norm'un 3 gerekli önþartý yerine getirip getirmediðine
bakalým. Bariz ki $||x|| \ge 0$ çünkü norm kesin deðer kullandýk ve kesin
deðerler hep sýfýrdan büyük, ayrýca $||x||$ sýfýr olmasý için $x(t)$'nin
her yerde sýfýr olmasý lazým, fonksiyon tek bir noktada sýfýrdan azýcýk
daha büyük olsaydý, $max$ hemen onu norm kabul ederdi. Üçgensel eþitsizlik
alttaki iliþkinin bir uzantýsý zaten

$$ 
\max |x(t) + y(t)| \le 
\max [|x(t)| + |y(t)|] \le
\max |x(t)| + max |y(t)|
$$

Üstteki eþitsizlikler maksimum fonksiyonun özellikleri, ve bu özellikler
onun üçgensel eþitsizliði de yerine getirmesini saðlýyor. 

En son olarak 3. önþart alttaki iliþkinin doðal sonucu olarak yerine
getirilmiþ oluyor 

$$ \max |\alpha x(t)|  = \max |\alpha||x(t)| = |\alpha| \max |x(t)|  $$

Örnek

$[a,b]$ aralýðýnda tanýmlý tüm sürekli fonksiyonlarýn uzayý alttaki norm
üzerinden bir norm edilmiþ uzaydýr

$$ ||x|| = \int _{ a}^{b} |x(t)| \ud t $$

Dikkat, bu norm edilmiþ uzay $C[a,b]$'den farklýdýr. 

Örnek 

Öklitsel n-uzayý ki $E^n$ olarak temsil edilir, ve norm'u $x = \{ \xi_1,
\xi_2, .., \xi_n\}$ için $||x|| = (\sum _{ i=1}^{n} |\xi_i|^2)^{1/2}$, bir
norm edilmiþ uzaydýr.

Yakýnsaklýk (Convergence)

Çoðu zaman, istenen bir özelliðe sahip olan bir vektörün varlýðýný ispat
ederken belli bir limite yaklaþan bir vektör dizisi yaratmak yaygýn bir
tekniktir. Çoðu zaman bu limitin istenen özelliðe sahip olduðu
gösterilebilir. Bu sebeple yakýnsaklýk (convergence) kavramý Analizde çok
önemli rol oynayan bir kavramdýr.

Taným 

Norm edilmiþ bir lineer uzayda sonsuz sayýda vektör içeren bir dizi
$\{x_n\}$'in $x$'e {\em yaklaþtýðý} söylenir eðer $\{||x_n-x||\}$ reel
sayýlar dizisi sýfýra yaklaþýyorsa. Bu durumda $x_n \to x$ diyebiliriz.

Eðer $x_n \to x$, $||x_n|| \to ||x||$ olmalý, çünkü (1)'e göre 

$$ ||x_n||  - ||x|| \le ||x_n-x|| $$

ya da, terimlerin yeri deðiþtirilmiþ halde

$$ ||x||  - ||x_n|| \le ||x-x_n|| $$

O zaman 

$$ \bigg| ||x||  - ||x_n|| \bigg|  \le ||x-x_n|| \to 0$$

olmalýdýr. 

Teori 

Eðer bir dizi yaklaþýyorsa, limiti özgündür (unique).

Ýspat

Diyelim ki $x_n \to x$ ve $x_n \to y$, yani $x_n$ apayrý iki limite
yaklaþýyor (gibi) bir þey ortaya attýk. Peki o zaman $||x-y||$ ne olur?
Göreceðiz ki bu norm sýfýra gidecek ve bu sebeple $x,y$'nin birbirinden
farklý olamayacaðýný anlamýþ olacaðýz. 

$$ ||x-y|| =  ||x-x_n + x_n-y|| $$

Üstte yine ayný numarayý kullandýk, $-x_n+x_n$ ekleyerek eþitlikte hiçbir
þey deðiþtirmiyoruz, ama daha fazla terim elde ederek þimdi üçgensel
eþitsizliði kullanabileceðiz. $+$ iþaretinin solundaki ve saðýndaki
bloklarý ayrý terimler gibi kabul edersek, 

$$ ||x-x_n + x_n-y|| \le ||x-x_n || + ||x_n-y|| $$

ve

$$ ||x-x_n || \to 0 $$

$$ ||x_n-y|| \to 0$$

olduðuna göre 

$$  ||x-y|| \to 0 $$ 

$\square$

\end{document}
