\documentclass[12pt,fleqn]{article}\usepackage{../../common}
\begin{document}
Ders 2

Süreklilik

Taným

$S \subset \mathbb{R}$, $f: S \to \mathbb{R}$ bir fonksiyon olsun, ve $c \in S$
bir sayý olsun. ``$f$'in $c$'de sürekli olduðu'' söylenir, þu durumda: Eðer her
$\epsilon>0$ için bir $\delta > 0$ var ise, öyle ki ne zaman $x \in S$ ve $|x-c|
< \delta$ iþe, o zaman $|f(x) - f(c)| < \epsilon$ doðrudur.

Eðer $f: S \to \mathbb{R}$ her $c \in S$ için doðru ise, yani bir $c$ noktasý
deðil tüm $c$'ler için geçerli ise, o zaman $f$'in sürekli olduðunu
söyleriz. Yani nokta belirtmeye ihtiyaç kalmaz.

Üstteki taným Analizde doðru anlaþýlmasý gereken en önemli teorilerden biridir,
ve tam anlamasý pek kolay olmayabilir. Dikkat edilirse $\delta$, hem
$\epsilon$'a hem de $c$'ye baðlý. Yani her $c \in S$ için ayný $\delta$
seçilmiyor.

Ayrýca sürekli fonksiyonlarýn tanýmýnýn limitlerin tanýmýna benziyor olmasý
raslantý deðil, sürekli fonksiyonlarýn önemli bir özelliði zaten onlarýn düzgün
limitleri olmalarý.

\includegraphics[height=4cm]{2_5.png}

Tanýmýn iþlemesinde mutlak deðer (absolute value, $||$ iþareti) kritik bir rol
oynuyor. Grafiksel olarak þöyle gösterebiliriz. Bir fonksiyonun deðerleri
etrafýnda, yukarý, aþaðý olmak üzere $\epsilon$ kadar bir pencere tanýmlýyoruz
(yeþil olarak görülen bölüm). Þimdi örnekte $c=2$ etrafýnda, yani $x$ bazýnda
öyle bir baþka pencere tanýmlayalým ki, bu penceredeki deðerler tamamen yeþil
kýsýma tekabül eden deðerlerin içinde kalsýn. Bu þekilde tek bir pencere
bulabildiðimiz anda iþ tamamdýr. Ve bunu tüm $\epsilon > 0$ için yapabiliyorsak,
o fonksiyon sürekli demektir.

\includegraphics[height=4cm]{2_4.png}

Diðer yandan üstteki grafikte gösterilen 

$$ 
f(x) = 
\left\{ \begin{array}{ll}
x < 5 & 2x \\
x \ge 5 & 3x
\end{array} \right.
$$

fonksiyonu sürekli deðildir. Eðer $c=5$ etrafýnda $\epsilon = 4$ alýrsak mesela,
bu pencereye tekabül eden bir $\delta$ bulamayýz. Fakat þu fonksiyon süreklidir.

\includegraphics[height=4cm]{2_3.png}

$$ 
f(x) = 
\left\{ \begin{array}{ll}
x < 5 & 2x \\
x \ge 5 & 3x + 10
\end{array} \right.
$$

Fonksiyonun pürüzsüz (smooth) olmadýðýna dikkat, yani iki parçalý bir fonksiyon,
kesikli bir þekilde tanýmlý, ama yine de sürekli.

Süreklilik için limitlere dayalý bir taným daha açýklayýcý olabilir [1,
sf. 125]. Bir iç nokta (interior point) $c$ için, $y=f(x)$ o noktada
sürekli denir eðer

$$ \lim_{x \to c} = f(c)$$

ise. Yani bir noktadaki fonksiyon limiti eðer fonksiyonun o noktadaki
deðerine eþit ise fonksiyon o noktada süreklidir.

\includegraphics[width=20em]{2_6.png}

Mesela üstteki parçalý fonksiyonda $x=3$ noktasýnda süreklilik vardýr (her
ne kadar kýrýlýþ varsa bile), çünkü o noktada

$$ \lim_{x \to 3} f(x) = f(3) $$

Eþit Süreklilik (Uniform Continuity) 

Süreklilik tanýmýnda $\delta$'nin $c$ noktasýna baðlý olduðunu söylemiþtik. Ama
bazý durumlarda $\delta$'nin baðýmsýz olmasý daha faydalýdýr.

Taným

$S \subset \mathbb{R}$, $f:S \to \mathbb{R}$ bir fonksiyon olsun. Farz edelim ki
her $\epsilon > 0$ için bir $\delta > 0$ mevcut, ki $x,c \in S$ ve $|x-c| <
\delta$ olduðu zaman, $|f(x) - f(c)| < \epsilon$.  Bu durumlarda fonksiyona eþit
sürekli denir.

Eþit Sürekli bir fonksiyonun (normal) sürekli bir fonksiyon olacaðýný görmek zor
olmaz. Buradaki tek fark her seçilen $\epsilon > 0$ için öyle bir $\delta > 0$
seçiyoruz ki bu $\delta$ her $c \in S$ için ise yarýyor. Yani bu yeni tanýma
göre artýk $\delta$, $c$'ye baðlý deðil, sadece $\epsilon$'a baðlý. Tanýmýn
yapýldýðý arka plan, bölge (domain) bir fark yaratacak. Daha büyük bir kümede
eþit sürekli olmayan bir fonksiyon, daha ufak bir küme içinde eþit sürekli
haline gelebilecek.

Lipschitz Sürekliliði 

Taným 

$f:S \to \mathbb{R}$ bir fonksiyon olsun, öyle ki $S$ içindeki her $x,y$ için
bir $K$ sayýsý mevcut, ve tüm bunlarla alttaki eþitsizlik doðru

$$ |f(x) - f(y)| \le K|x-y| $$

O zaman $f$'e Lipschitz Sürekli adý verilir. 

Çok geniþ bir fonksiyon kategorisi Lipschitz süreklidir. 

Aslýnda Lipschitz fonksiyonlarý fonksiyonun türevi için bir üst limit
tanýmlar, eðer üstteki ifadeyi þu þekilde yazarsak,

$$ \left|\frac{f(x) - f(y)}{|x-y|} \right| \le K $$

her $x,y$ için üstteki hesabýn daha az olacaðý bir $K$ vardýr diyoruz ve bu
$K$ fonksiyonun her noktasýnda türevi için bir üst sýnýr olacaktýr. Alttaki
gibi bir resim üzerinde anlatýrsak,

\includegraphics[width=20em]{2_7.png}

söylediðimiz fonksiyonun hep beyaz koni dýþýnda kalacaðýnýn
garantisidir. O koni dýþýnda kalmak ta dolaylý olarak eðimin çok aþýrý
büyük olmamasý anlamýna geliyor. Yani demek istiyoruz ki bu fonksiyon
``patlamayacak''. Örnek olarak $\sin(x^2)$, ya da $\sin(1/x)$ Lipschitz
deðildir. Pek çok polinom, ``normal'' fonksiyon Lipschitz'dir. Ýlla türevin
mevcut olmasý bile gerekmez, mesela $f(x) = |x|$'in her noktada türevi
yoktur ama Lipschitz'dir/ 

Fakat dikkat edelim, aynen eþit süreklilikte olduðu gibi Lipschitz
sürekliliðinde de fonksiyonun tanýmlandýðý bölge (domain) çok
önemlidir. Þimdi ``sürekli'' kelimesini kullanmamýzýn doðruluðunu kontrol
edelim.

Teori 

Her Lipschitz sürekli fonksiyon, ayný zamanda eþit sürekli bir fonksiyondur.

Ýspat

$f: S \to \mathbb{R}$ olduðunu kabul edelim, ve öyle bir $K$ sayýsý olsun ki $S$
içindeki her $x$ ve $y$ için $f(x) - f(y)| \le K|x-y|$. Bu Lipschitz süreklilik
tanýmýnýn bir tekrarý. Bir $\epsilon > 0$ seçelim. Sonra $\delta = \epsilon / K$
alalým.  $|x-y| < \delta$ olacak þekilde her $x,y \in S$ için

$$ |f(x) - f(y)| \le K|x-y| < K\delta = K \frac{ \epsilon}{K} = \epsilon $$

Birinci eþitsizlik Lipschitz tanýmýndan geliyor. Bu eþitsizliðin sað tarafýnda
diðer bildiklerimizi yerine koyunca, $\epsilon$ elde ediyoruz.

Tamlýk (Completeness) 

Taným

Bir metrik uzayý $(X,d)$ tamdýr (complete) eðer $X$ alanýndaki her Cauchy serisi
(o da $X$ içinde olan) bir öðeye yaklaþýyor ise.

Üstteki tanýmý önceki dersteki Cauchy tanýmý ile birleþtirirsek, $\mathbb{R}$
uzayýnýn ``tam'' olduðunu görebiliriz. Çünkü her Cauchy dizisinin
$\mathbb{R}$'de yakýnlaþtýðýný biliyoruz, ayrýca bir bir reel sayýya
yaklaþýldýðýný biliyoruz. Bu reel sayý $L$'in kendisi de zaten $\mathbb{R}$
içinde olduðuna göre, $\mathbb{R}$ uzayý tamdýr.

Inf ve Sup

Sup

Eðer $S$ kümesi ``yukarýdan sýnýrlanmýþ (bounded from above)'' ise o zaman $x
\in S$ için öyle bir $y$ var demektir ki her $x$ için $x \le y$ olsun. Yani $S$
içindeki her deðer bu $y$ deðerinden küçük olsun. Bu $x$ deðerine $S$'in
supremum'u da deniyor, ve $\sup\limits_{x \in S}(x)$ ya da $sup\{x:x \in S\}$
olarak gösterilebiliyor.

Inf

Benzer þekilde kümenin en alt sýnýrý, yani infimum deðeri $\inf\limits_{x \in
 S}(x)$ ya da $inf\{x:x \in S\}$ olarak gösteriliyor.

Eðer elimizde bir seri (sequence) var ise o zaman þartlarý biraz daha gevþetmek
iyidir, burada limit superior kavramý devreye girer. Inf ve sup deðerleri altý /
üstü deðer olamaz, ama limit superior öyle bir sayýdýr ki onun sonrasýnda sonlu
(finite) / belli sayýda küme öðesi olmasýna izin verilir. Limit superior aslýnda
bir serinin yakýnsadýðý (converge) deðerden baþkasý deðildir.

Bu kavramlarýn minimum ve maksimum kavramlarýndan farký ne? Inf ve sup bir küme
{\em dýþýnda da} olabilirler. Bir kümenin minimal deðeri muhakkak o küme içinde
olmalý ama öyle kümeler vardýr ki minimal ya da maksimal deðeri yoktur. Mesela
$\mathbb{R}+$ yani sýfýr hariç tüm pozitif reel sayýlarý düþünelim: minimumu
nedir? Hangi ``çok küçük'' deðeri alýrsak alalým, o deðeri iki ile bölerek daha
küçük bir deðer elde edebilirim, yani minimum yoktur. Fakat bu kümenin bir
infimumu vardýr, sýfýr deðeri. Sýfýr bu küme içinde deðildir ama kümeyi
sýnýrlayan bir deðerdir.

Benzer örnek ters yönden supremum için de geçerli. 

\includegraphics[height=1.5cm]{2_1.png}

\includegraphics[height=3cm]{2_2.png}

Formel olarak diyelim ki $\{x_n\}$ bir seri, ve diyelim ki bir reel sayý $S$
var, ki bu reel sayý þu þartlarý tatmin ediyor 1) Her $\epsilon > 0$ için bir
$N$ var, öyle ki her $n>N$ için $x_n < S + \epsilon$ ve 2) her $\epsilon > 0$ ve
$M>0$ için bir $n>M$ var ki $x_n > S - \epsilon$. O zaman $S$ sayýsýna $\{x_n\}$
serisinin limit superior'u denir.

Bu tanýmýn söylemeye çalýþtýðý serinin yaklaþtýðý deðerden sonra ve önce sonlu
büyüklükte bir pencere tanýmlarsak bu pencere içinde sonlu sayýda eleman
olacaktýr (sonsuz deðil). Bu pencerenin tanýmlanabiliyor olmasý, onun makul bir
noktada olmasýný gerektirir, ki bu nokta da yaklaþýlan deðerden baþkasý
deðildir.

Limit inferior bunun tersidir, 

$$ \lim \inf x_n  = -\lim \sup(-x_n)$$

Vektör Uzaylarý 

Her vektör uzayýyla ilintili olan bir tek sayý / skalar (scalar) kümesi vardýr,
ve bu büyüklükler ile o uzayda çarpým iþlemi tanýmlanýr. Soyut baðlamda
çalýþýlanlar için bu büyüklüklerin cebirsel bir alan (algebraiç field -bir soyut
matematik kavramý-) üyesi olmasý yeterlidir. Fakat bu notlarda kullanacaðýmýz
büyüklükler ya reel sayýlar, ya da kompleks sayýlar olacak. Bu iki olasýlýk
arasýnda hangisini kullandýðýmýzý belli etmek için vektör uzayýna ``reel vektör
uzayý'' ya da ``kompleks vektörü uzayý'' diyebiliriz. Odaðýmýz ise çoðunlukla
reel vektör uzaylarý olacak, kompleks olanlarý nadir kullanacaðýz. Yani eðer
uzayýn þekli söylenmemiþse, onun reel olduðunu farz edin.

Taným 

Vektör uzayý $X$, ``vektör'' denen öðeleri içeren bir küme, artý iki
operasyondan oluþur. Ýlk operasyon toplama, diðeri çarpmadýr. Toplama iþlemi iki
vektör $x,y \in X$'i bir diðer vektör $x+y \in X$ ile baðdaþtýrýr. Çarpma iþlemi
$x \in X$ ve herhangi bir sayý, skalar $\alpha$ ile vektör $\alpha x$'i
baðdaþtýrýr.

$\theta$ sýfýr vektörüdür. 

$$  0 \ x = \theta, \ 1 \ x = x $$

[diðer önþartlar atlandý, sýrabaðýmsýzlýk (commutative) kuralý, vs, toplam 7
  tane]

Örnek

Herhalde vektör uzaylarýna verilecek en basit örnek reel sayýlar kümesidir. Bu
durumda küme elemanlarý olan ``vektörler'' tek boyutludur. Vektörü uzayý (doðal
olarak) bir reel vektör uzayýdýr, toplama, çarpma reel sayýlarýn üzerinden
tanýmlýdýr. Sýfýr vektörü $\theta$, sýfýr sayýsýdýr. Bu uzaya reel kordinat
uzayý, ya da basit ifadeyle ``reel çizgi (real line)'' adý da verilebilir,
$R^1$, ya da $R$ olarak gösterilir.

Kaynaklar

[1] Thomas, {\em Thomas' Calculus, 11th Edition} 

[2] Wikipedia, {\em Lipschitz continuity}
    \url{https://en.wikipedia.org/wiki/Lipschitz_continuity}

\end{document}



