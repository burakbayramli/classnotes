\documentclass[12pt,fleqn]{article}\usepackage{../../common}
\begin{document}
Giriþ

Bu yazýda ileride hocanýn tam deðinmediði konularý tanýþtýrmaya uðraþacaðýz.

Vektörler

Bir vektör bir yön ve büyüklüktür, baþlangýç noktasý olarak genellikle orijin
kabul edilir. 1,2, ya da daha fazla boyutta bir sayý listesi ile gösterilebilir,
tek boyutta ise bu tabii ki bir skalar ile ayný þeydir [5]. 

Toplamak

Ýki boyutta bir örnek görelim, iki vektör $a,b$ var, toplamlarý

\includegraphics[width=15em]{vector_a_plus_b.png}

Toplam sanki birinci vektörü alýp diðerinin bittiði yerden baþlatmak gibi,
varýlan yeri gösteren yeni vektör toplam vektörüdür. Çýkartmak ise ayný yerden
baþlayan iki vektörde birinin sonundan diðerine sonuna giden vektörü bulmak
gibidir. Aslýnda toplam üzerinden çýkartma doðrulabilir, $a-b$ düþünürken $-b$
diye yeni bir vektör yaratýrýz (yani vektörü tersine çeviririz) ve $a+(-b)$ ile
toplam iþlemini yaparýz.

Çýkartmak

\includegraphics[width=15em]{vector_b_minus_a.png}

Kosinüsler Kanunu (Law of Cosines)

Þöyle bir üçgen olduðunu düþünelim [1],

\includegraphics[width=15em]{cos1.png}

Eðer, mesela C'nin 90 derece olduðunu bilseydik, Pitagor kuralýndan

$$
c^2 = a^2 + b^2
$$

diyebilirdik. Ama $C < 90$ ise, farklý bir formül kullanýlabilir,

$$
c^2 = a^2 + b^2 - 2 a b \cos C
$$

Eðer $C=90$ ise $\cos(90) = 0$ olduðu için Pitagor kuralýný elde ettiðimizi
görürüz. 

Üstteki kurala Kosinüsler Kanunu denir, ispatý þöyle,

\includegraphics[width=15em]{cos2.png}

Üstteki üçgene bakarsak,

$$
c^2 = (a \sin C)^2 + (b-a\cos C)^2
$$

$$
= a^2 \sin^2 C + b^2 - 2 a b \cos C + a^2 \cos^2 C
$$

$$
c^2 = a^2 + b^2 - 2 a b \cos C
$$

Noktasal Çarpým

Ýki boyutta iki vektör $\vec{a},\vec{b}$ olduðunu düþünelim. Bu vektör ögelerini
$\vec{a} = [a_1, a_2]$ ve $\vec{b} = [b_1, b_2]$ diye gösterirsek, noktasal
çarpým $\vec{a} \cdot \vec{b}$ her iki vektörün tekabül eden öðelerinin
çarpýmýnýn toplamýdýr, yani

$$
\vec{a} \cdot \vec{b} = a_1 b_1 + a_2 b_2
$$

Daha yüksek boyutlarda benzer iþlem devam ettirilir. 

Norm

Bir vektorun uzunlugunun, buyuklugunun, ya da normu, ki $||\vec{a}||$ ile
gosterilebilir, karesi

$$
||\vec{a}||^2 = \vec{a} \cdot \vec{a} 
$$

Bu kurallara uygun çünkü

$$
(\vec{a} \cdot \vec{a})^2 = a_1 a_1 + a_2 a_2
$$

Ya da

$$
||\vec{a}|| = \sqrt{\vec{a} \cdot \vec{a}} = \sqrt{a_1 a_1 + a_2 a_2}
$$

Aslýnda büyüklük formülü Pitagor üçgen formülünden hareketle de anlaþýlýr çünkü
herhangi bir vektör düþünürsek, $a_1,a_2$ ya da $x$ ekseninde $a_1$ $y$
ekseninde $a_2$ olan bir nokta düþünürsek bu noktaya olan orijinden olan uzaklýk
(vektör) Pitagor formülü ile hesaplanabilirdi, $\sqrt{a_1^2 + a_2^2}$.

Noktasal çarpým þu þekilde de hesaplanabilir,

$$
\vec{a} \cdot \vec{b} = ||\vec{a}|| ||\vec{b}|| \cos\theta
$$

Ýspat

Alttaki þekle bakarsak,

\includegraphics[width=15em]{abcos.png}

Bu üçgenin kenarlarý arasýnda Kosinüsler Kanunu uygularsak,

$$
||\vec{a}-\vec{b}||^2 =
||\vec{a}||^2 + ||\vec{b}||^2 -
2 ||\vec{a}|| ||\vec{b}|| \cos\theta
\mlabel{1}
$$

Norm açýlýmýndan hareketle

$$
||\vec{a}-\vec{b}||^2 = (\vec{a}-\vec{b}) \cdot (\vec{a}-\vec{b}) 
$$

Noktasal çarpýmý parantezler üzerinden açarsak,

$$
= \vec{a}\cdot\vec{a} - \vec{a}\cdot\vec{b} - \vec{b}\cdot\vec{a} + \vec{b}\cdot\vec{b}
$$

$$
= ||\vec{a}||^2 - 2\vec{a}\cdot\vec{b} + ||\vec{b}||^2
$$

(1) formülünde sol tarafý üstteki formülle açarsak,

$$
||\vec{a}||^2 - 2\vec{a}\cdot\vec{b} + ||\vec{b}||^2 =
||\vec{a}||^2 + ||\vec{b}||^2 - 2 ||\vec{a}|| ||\vec{b}|| \cos\theta
$$

$$
- 2\vec{a}\cdot\vec{b} = - 2 ||\vec{a}|| ||\vec{b}|| \cos\theta
$$

$$
\vec{a}\cdot\vec{b} = ||\vec{a}|| ||\vec{b}|| \cos\theta
$$

Üstteki eþitlik veri analizinde kullanýþlý bulunmuþtur, mesela kimisi
müþterileri $D$ boyutunda vektörler olarak temsil edebilir, 1. öðe yaþ, 2. öðe
boy, vs. gibi bilgiler olabilir ve iki vektörün birbirine ne kadar ``yakýn''
olduðu iki vektörün arasýndaki açýsal uzaklýk üzerinden ölçülebilir,

$$
\cos\theta = \frac{\vec{a}\vec{b}}{||\vec{a}|| ||\vec{b}||}
$$

Eðer vektörler baþtan normalize edilmiþ ise bölme iþlemine de gerek kalmaz, tek
bir noktasal çarpým ile kabaca bir benzerlik ölçütü elde edilmiþ olur.

Diklik

Ýki vektörün birbirine dik olup olmadýðý (orthogonality) testi de noktasal
çarpým ile yapýlýr. $\cos(90) = 0$ olduðuna göre $\vec{a}\cdot\vec{b}$ çarpýmý
sýfýr ise iki vektör dik demektir. 

Yansýtma (Projection)

Vektör $\vec{a}$'nin $\vec{b}$ yönündeki büyüklüðü, yansýmasý nedir sorusunun
cevabý da noktasal çarpým ile bulunabilir, 

$$
\vec{a}\cdot\vec{b} = ||\vec{a}||||\vec{b}||\cos\theta
$$

formülünü biraz manipüle edersek cevabý alabiliriz, 

$$
\frac{\vec{a}\cdot\vec{b}}{||\vec{b}||} = ||\vec{a}|| \cos \theta
$$

\includegraphics[width=15em]{dot_product_projection.png}

Eþitliðin sað tarafý $||\vec{a}|| \cos \theta$ aradýðýmýz büyüklük, onu elde
etmek için $\frac{\vec{a}\cdot\vec{b}}{||\vec{b}||}$ noktasal çarpýmýný
kullanabileceðimizi görüyoruz [4].

Eðer büyüklüksel, skala yansýmasýný bulabiliyorsak, bir vektörün diðeri
yönündeki tamamen yansýmasýný da bulabilirdik, $\vec{b}$ yönünü
$\frac{\vec{b}}{||\vec{b}||}$ birim vektörü ile gösteriyoruz, o yöndeki
$\vec{a}$ büyüklüðünü biraz önce bulduk, $\frac{\vec{a}\cdot\vec{b}}{||\vec{b}||}$.
Yönü büyüklük ile çarpýnca istenilen vektör elde edilmiþ olur, $\vec{a}$'nin
$\vec{b}$ yönündeki yansýmasý $\proj_{\vec{b}} \vec{a}$

$$
\proj_{\vec{b}} \vec{a} = \frac{\vec{a}\cdot\vec{b}}{||\vec{b}||} \frac{\vec{b}}{||\vec{b}||}
$$

$$
= \frac{\vec{a}\cdot\vec{b}}{||\vec{b}||^2} \vec{b}
$$



Kaynaklar

[1] \url{https://mathworld.wolfram.com/LawofCosines.html}

[2] \url{https://tutorial.math.lamar.edu/classes/calcii/dotproduct.aspx}

[3] \url{http://sites.science.oregonstate.edu/math/home/programs/undergrad/CalculusQuestStudyGuides/vcalc/dotprod/dotprod.html}

[4] \url{https://mathinsight.org/dot_product}

[5] \url{https://mathinsight.org/vector_introduction}

\end{document}
