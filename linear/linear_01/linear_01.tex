\documentclass[12pt,fleqn]{article}\usepackage{../../common}
\begin{document}
Ders 1

Ýlk dersimize hoþgeldiniz, ben Gilbert Strang. Lineer cebirin çözmeye
çalýþtýðý en temel problem bir lineer denklem sistemini çözmektir. Bu
baðlamda mesela en genel durum ``bilinmeyen ve denklem sayýsýnýn birbirine
eþit olduðu'' durumdur, ki bu ``güzel durum'' olarak nitelenebilir. Boyut
$n \times n$ olunca bu durumda oluyoruz. Ama diðer durumlarý da
iþleyeceðiz. 

Dersin kavramlarýný anlamak için  ``satýr bakýþý''na baþvuracaðýz, bu
durumda her denkleme teker teker bakýyoruz gibi olacak, dersimizde pek çok
kez kullanacaðýmýz $A$ matrisimiz olacak mesela, ve bu matrisin satýrlarýn
her denklemin deðiþkenlerinin katsayýlarýna tekabül edecek.

Kolon bakýþ açýsý belki daha önce görmediðiniz bir açý olacak, bu durumda
her kolon ayrý ayrý iþlenecek. 

Cebirsel bakýþ açýsý ise tüm matrisi, bu durumda $A$, ayný anda ele alacak.

Güzel durumdan baþlayalým; iki bilinmeyen, iki denklem.

$$ 2x - y = 0 $$

$$ -x + 2y = 3 $$

Katsayýlarý matrise taþýyalým

$$ 
\left[\begin{array}{cc}
2 & -1 \\
-1 & 2
\end{array}\right]
\left[\begin{array}{c}
x \\
y
\end{array}\right]
=
\left[\begin{array}{c}
0 \\
3
\end{array}\right]
 $$

Soldaki matrise ben çoðunlukla $A$ derim, sonra içinde bilinmeyenleri
taþýyan $x$ vektörü koyarým, ki bazýlarý bunu vektör olduðu için köyü
renkte $\mathbf{x}$ olarak ta yazar, ve sonra bir vektör daha, ki buna ben
çoðunlukla $b$ derim, yani sonuç þöyle olur,

$$ A x = b $$

Þimdi satýr bakýþýna baþvuralým. Grafik olarak düþünelim, hangi noktalar
$2x - y = 0$ denklemini tatmin eder? Orijin $(0,0)$ noktasý bunlardan biri,
bir diðeri $x=1,y=2$. Bunlarý çizgi ile birleþtirip çizgiyi sonsuza uzatýrsak,

\includegraphics[height=4cm]{1_01.png}

Ýkinci denklemi düþünelim, bu denklem orijinden geçmeyecek. $y$ sýfýr
olsaydý $x$ nereden geçerdi..? $x=-3$ noktasýndan. $x=-1$ iþe, $y$ nedir?
$y=1$. Artýk ikinci çizgiyi çizebiliriz,

\includegraphics[height=4cm]{1_02.png}

Her iki çizgi $x=1,y=2$ noktasýnda buluþtu (hoca o noktayý daha önceden
tahtaya koymuþtu ama buna bir ``hazýr raslantý'' diyelim). Bu buluþma
noktasý her iki denklemi tatmin eden sistemin çözüm noktasýdýr.

Kolon bakýþýna gelelim. Bu bakýþa göre

$$ 
x
\left[\begin{array}{r}
2 \\
-1
\end{array}\right]
+
y
\left[\begin{array}{r}
-1 \\
2
\end{array}\right]
=
\left[\begin{array}{r}
0 \\
3
\end{array}\right]
$$

Bu bakýþ açýsý bize ne söylüyor? Bir anlamda diyor ki ``eþitliðin solundaki
iki vektörü öyle bir þekilde kombine et ki bu lineer kombinasyon sonucu
eþitliðin sað tarafýný versin''. Bu operasyon, iþlem lineer cebir dersinin
en temel iþlemidir - kolonlarýn lineer kombinasyonu. 

Kolonlarý vektörel olarak çizelim, bilindiði gibi bir vektör orijinden
çýkan bir yönü ve büyüklüðü olan bir þeydir. Birinci kolon 2 saða 1 aþaðý
gitmeli, ikincisi bir sola 2 yukarý gitmeli. Çizince,

\includegraphics[height=4cm]{1_03.png}

Peki bu vektörleri nasýl ``birleþtirelim'' yani hangi katsayýlarla çarpýp
toplayalým ki $\left[\begin{array}{rr}3 & 0\end{array}\right]^T$ sonucunu
elde edelim? Not: Burada devrik iþaretini kullandýk çünkü satýr vektörünün
devriðini alýnca kolon vektörü olur, yazarken o þekilde yazdýk çünkü baþka
türlü yazý içine rahat koymak mümkün olmayacaktý.

Neyse, kombine etmek toplamaktýr, o zaman ikinci kolonu alýp birinciye
ekleyelim, vektör aritmeðitinde toplamak bir vektörü diðerinin bittiði
noktadan baþlatmak demektir, bir tane (yani katsayý 1 ile) ikinci vektörü
birinciye ekleyince

\includegraphics[height=4cm]{1_04.png}

Çözüm noktasýna eriþemedik daha, 2 tane ekleyince,

\includegraphics[height=4cm]{1_05.png}

Çözüme eriþtik. Çözüm $y$ ekseni üzerinde 3,0 noktasý. Bu noktaya bir
lineer kombinasyon yaparak eriþtik. ``Doðru'' lineer kombinasyon bize
sonucu verdi. 

Kendimize þunu soralým: {\em bütün} lineer kombinasyonlar bize neyi
verirdi? Bu çok önemli bir soru, üzerinde iyi düþünelim, çünkü bu konu
tekrar tekrar karþýmýza çýkacak. Tüm kombinasyonlar derken tüm $x,y$'lerin
doðuracaðý kombinasyonlar; bu þekilde eþitliðin sað tarafý ne olursa olsun
onu temsil edebilirdik. Yani bu tüm kombinasyonlar tüm bir düzlemi (plane)
doldururdu. Bunu bir kenara koyalým, konuyu ileride daha detaylý olarak
inceleyeceðiz.

3 Boyut

$$  2x - y = 0  $$

$$ -2x + y - z = -1 $$

$$ -3y + 4z = 4 $$

Bu sistemi nasýl anlarýz? Ne gördük: satýr bakýþý, kolon bakýþý. Matris
formuna bakalým, 

$$ A = 
\left[\begin{array}{rrr}
2 & -1 & 0 \\
-1 & 2 & -1 \\
0 & -3 & 4 
\end{array}\right]
,
b = 
\left[\begin{array}{r}
0  \\
-1 \\
4  
\end{array}\right]
 $$

Satýr bakýþý, üstteki ikinci denklemi alalým, grafiði neye benzer?
Orijinden geçen bir þey yok, çünkü $0,0,0$ bir çözüm olamaz. Çözüm neye
benzer? Elimizde bir lineer denklem var ise üç boyutta bu denklemi çözen
tüm noktalar bir düzlem (plane) oluþtururlar. Hoca diyor ki ben Rembrandt
deðilim, kabaca bir düzlem çizeceðim, 

\includegraphics[height=4cm]{1_06.png}

Birinci denklem neye benzer? $z$ belirtilmemiþ, demek ki herhangi bir þey
olabilir, geri kalanlar yine bir düzlem oluþtururlar. Mesela alttaki gibi

\includegraphics[height=4cm]{1_07.png}

Bu iki denklemin kesiþtiði yer bir çizgidir (line). Bir üçüncü düzlem daha
eklenince (üstte yok), tüm üç düzlemin kesiþtiði yer bir nokta olur. Bu
noktanýn ne olduðunu ben kafadan bilmiyorum, ama lineer cebir bu noktayý
bulur.

Fakat herhalde görüyorsunuz, satýr bakýþýný zihnimizde canlandýrmak biraz
zor. Düzlemler, kesiþmeler vs düþünmemiz gerekiyor. Ýki boyutta iki çizgi
kesiþmesinde problem yoktu, fakat üç boyutta iþler biraz karýþtý. Dört ya
da daha fazla boyutu düþünün! 

Kolon bakýþý ile

$$ 
x 
\left[\begin{array}{r}
2 \\
-1 \\
0
\end{array}\right]
+
y
\left[\begin{array}{r}
-1 \\
2 \\
-3
\end{array}\right]
+
z 
\left[\begin{array}{r}
0 \\
-1 \\
4
\end{array}\right]
=
\left[\begin{array}{r}
0 \\
-1 \\
4
\end{array}\right]
 $$

Yani eþitliðin sol tarafýndaki üç boyutlu vektörlerin hangi kombinasyonu
eþitliðin saðýndaki üç boyutlu vektörü verir? Çizmeye uðraþalým,

\includegraphics[height=4cm]{1_08.png}

Hocanýn hazýrladýðý örnekte basit bir çözüm var, $z$ katsayýlarý ile $b$
ayný, o zaman $\left[\begin{array}{ccc}0&0&1\end{array}\right]$ denklemi çözer,
$z=1$ çözüm için yeterli olur, $z$'yi alýrýz, geri kalanlarý ``istemiyoruz''
onlarý sýfýr yaparýz - $x=0,y=0,z=1$.

Tabii kolon resminin daha düzgün olmasý her zaman çözümü pat diye
görebileceðimiz anlamýna gelmiyor, çoðu zaman, hatta yüksek boyutlarda bu
da mümkün deðil. Eliminasyon yöntemini göreceðiz ileriki derslerde,
sistematik olarak herhangi bir boyutta bizim için lineer sistemi çözecek. 

Daha geniþ resme bir bakalým. Eþitliðin sað tarafý baþka bir þey olsaydý,
mesela

$$ 
x 
\left[\begin{array}{r}
2 \\
-1 \\
0
\end{array}\right]
+
y
\left[\begin{array}{r}
-1 \\
2 \\
-3
\end{array}\right]
+
z 
\left[\begin{array}{r}
0 \\
-1 \\
4
\end{array}\right]
=
\left[\begin{array}{r}
1 \\
1 \\
3
\end{array}\right]
 $$

(Bu sað tarafý 1. ve 2. kolonlarý birbirine ekleyerek uydurduk bu arada),
çözüm o zaman ne olurdu? Çözüm basit (çünkü kendimiz piþirdik),
$x=1,y=1,z=0$. Üstteki durumda ve satýr bakýþýnda tamamen deðiþik üç tane
düzlem elde ederdik. Kolon bakýþýnda hala ayný üç vektörle uðraþýrýz,
sadece onlarý deðiþik þekillerde kombine etmemiz gerekir. 

Soru: üstteki denklemi herhangi bir $b$ yani eþitliðin sað tarafý için
çözebilir miyim? 

Bu soruyu daha deðiþik bir þekle sokabilir miyiz, öyle ki soru lineer
kombinasyon argümanlarýný kullansýn. 

Soru: üstteki kolonlarýn lineer kombinasyonu bütün 3-boyutlu uzayý doldurur
mu?

Cevap, üstteki (gibi bir) matris için ``evet''. Bu matris nasýl bir matris?
Bu matris iyi huylu, eþsiz (singular) olmayan, tersi alýnabilir
(ýnvertible) bir matris. Bu tür matrisler için her zaman çözüm var. 

Peki iyi huylu olmayan durum nasýl olurdu? Düþünelim, ne yanlýþ
gidebilirdi? Eðer tüm vektörler ayný düzlem üzerinde olsaydýlar, o zaman bu
vektörlerin her türlü kombinasyonu da ayný düzlem üzerinde olmak
zorundadýr, o zaman baþýmýz dertte. Mesela 3. kolon 1. ve 2.'nin toplamý
olsa (ki bu durumda her üç vektör ayný düzlemdedir), o zaman iþler
kötü. Niye? Çünkü $b$ farklý bir düzlemde ise artýk hiçbir kombinasyonla
oraya eriþemem. Eðer $b$ ayný düzlem üzerinde olsaydý tabii eriþilebilirdi,
yani {\em bazý} $b$'ler için çözüm hala var, ama hepsi, çoðunluðu için yok. 

Ýþte eþsiz durum budur, ki bu durumda çoðu $b$ için çözüm yoktur.

Boyutlarý arttýrdýðýmýzý düþünelim. 9 boyut olsaydý ne olurdu? Bu kadar
boyutu görsel olarak hayal etmek mümkün deðil, ama yine ayný durum, $Ax=b$
var, bu sefer 9 boyutlu 9 tane kolon var, bunlarý kombine edip $b$ elde
edeceðiz? Bunu yapmayý baþarabilmemiz tabii ki $A$'nin ne olduðuna
baðlýdýr. Rasgele bir $A$ matrisi üretseydim (ki bu Numpy \verb!rand! ile
basitçe yapýlabilir) size garanti ediyorum bu matris iyi huylu olurdu,
eþsiz olmayan tersi alýnabilen vs. ve bu matris ile $b$'ye
eriþebilirdiniz. 

Kötü durum kolonlar baðýmsýz olmadýðý zaman. Mesela 7. kolon 5. kolonun
týpatýp aynýsý, ki bu durumda 7. kolon sisteme ``yeni hiç bir bilgi
eklemiyor''. Bu yüzden bazý $b$'lere eriþmem imkansýz hale geliyor. Ýyi
huylu durumda 9 baðýmsýz kolonun kombinasyonu tüm 9 boyutlu uzayý
doldurabilirdi. Ama bir baðýmlý vektör var ise o zaman sadece 9 boyutlu
uzayýn 8 boyutlu bir alt düzlemini doldurabilirsiniz. Dersimizin ileri
noktalarýnda bu kýsýtlý alt uzaylar ile de iþ yapmayý öðreneceðiz, yapmamýz
gerekecek. Þimdilik güzel koþula odaklanalým.  

Ayrýca, þimdiye kadar sözel olarak belirttiklerimizi bir de matris
formunda, cebiriyle anlatmayý deneyelim. $Ax=b$ dedik, $A$ matrisi bir $x$
vektörü ile çarpýlýr. Bir vektörü bir matris ile nasýl çarparsýnýz? Mesela

$$ 
\left[\begin{array}{rr}
2 & 5 \\
1 & 3
\end{array}\right]
\left[\begin{array}{r}
1 \\
2
\end{array}\right]
 $$

Bunu yapmanýn iki yolu var. Önce benim en favori yöntemimi söyleyeyim,
belki de sürpriz olmayacak, kolonlar kullanarak. Bu durumda, mesela üstteki
örnekte, $A$'nin 1. kolunundan 1 tane, 2. kolonundan 2 tane alacaðým, ve
toplayacaðým. Yani,

$$ =
1
\left[\begin{array}{r}
2 \\
1
\end{array}\right]
+
2
\left[\begin{array}{r}
5 \\
3
\end{array}\right]
=
\left[\begin{array}{r}
12 \\
7
\end{array}\right]
 $$

Diðer bir yöntem satýr bazlý. Bu durumda $A$'nin 1. satýrý ile $x$'i
çarpýyorum, yani noktasal çarpýmýný alýyorum, ve bu tek bir deðer sonucunu
verecek, ve tek, skalar deðeri alýp sonucun ilk hanesine yazýyorum. Ayný
þekilde $A$'nin 2. satýrýný tekrar ayný $x$ ile noktsal çarpýyorum, ikinci
haneye yazýyorum. 

Fakat daha önce belirttiðimiz gibi kolon bakýþý biraz daha rahat
açýklanabilir halde. O zaman þunu söyleyebiliriz: ``$Ax$, $A$'nýn
kolonlarýnýn bir kombinasyonudur''. 

Bir sonraki derste herhangi boyuttaki bir sistem için sistematik olarak
eliminasyon yaparak, eðer varsa, bir sonuca ulaþmayý öðreneceðiz. Eðer
eliminasyon baþarýsýz olursa bunun üzerinden ne zaman bir sonuç olmadýðýný
görmeyi de öðreneceðiz. 

Ekler

Matris Çarpýmý

Matrix çarpýmýnýn tarifini lise derslerinden hatýrlayabiliriz. Sol el
sol taraftaki matriste bir satýr boyunca, sað el saðdaki matris
üzerinde kolon boyunca öðe öðe hareket ettirilir, ve bu hareket
sýrasýndaki öðeler çarpýlýp, o çarpýmlar sürekli toplanýr. Sol ve sað
elin bir hareketi bittiðinde, ele geçen tek bir sayý vardýr, ve o sayý
üzerinden geçilen satýr $i$ ve kolon $j$ için sonuç matrisi, mesela
$C$'nin, $i$'inci satýrý ve $j$'inci kolonuna yazýlýr.

Daha basit bir $Ax$ örneðine bakarsak, yani solda $A$ ve saðda $x$
var, çarpým

$$
\left[\begin{array}{ccc}
1 & 1 & 6 \\
3 & 0 & 1 \\
1 & 1 & 4 \\
\end{array}\right]
\left[\begin{array}{ccc}
2  \\
5  \\
0  \\
\end{array}\right]
$$

Noktasal Çarpým Bakýþý

Bu çarpýmý bir kaç þekilde görebiliriz. Eðer üstte tarif edilen gibi gördüysek,

$$
\left[\begin{array}{ccc}
1\cdot 2 + 1\cdot 5 + 6\cdot 0 \\
3\cdot 2 + 0\cdot 1 + 3\cdot 0 \\
1\cdot 2 + 1\cdot 5 + 4\cdot 0 
\end{array}\right]
=
\left[\begin{array}{c}
7 \\
6 \\
7 \\
\end{array}\right]
$$

Kolonsal Kombinasyon Bakýþý

Fakat matris çarpýmýna bakmanýn bir yolu daha var, hatta bu bakýþ açýsýnýn daha
önemli bile olduðu söylenebilir, o da $A$'nin kolonlarýnýn kombine edilerek
saða sonuç olarak geçilmesi bakýþýdýr. Buna göre

$$ 
2\cdot 
\left[\begin{array}{ccc}
1 \\
3 \\
1 \\
\end{array}\right]
+
5\cdot 
\left[\begin{array}{c}
1 \\
0 \\
1 \\
\end{array}\right]
+
0\cdot 
\left[\begin{array}{c}
6 \\
3 \\
4 \\
\end{array}\right]
=
\left[\begin{array}{c}
7 \\
6 \\
7 \\
\end{array}\right]
$$

Tabii burada ikinci "matris" aslinda bir vektör, ama o vektör de matris
olsaydý,

\begin{minted}[fontsize=\footnotesize]{python}
A = np.array([[1 ,1 , 6],[3 , 0 , 1],[1 , 1 , 4]])
x = np.array([[2], [5], [0]])
print ('vektor ile\n')
print (np.dot(A,x))
B = np.array([[2, 2, 2],[5, 5, 5],[0, 0, 0]])
print ('\nmatris ile\n')
print (np.dot(A,B))
\end{minted}

\begin{verbatim}
vektor ile

[[7]
 [6]
 [7]]

matris ile

[[7 7 7]
 [6 6 6]
 [7 7 7]]
\end{verbatim}

Satýr Kombinasyon Bakýþý

Saðdan çarpan vektörü bir geniþleterek 2 boyutlu hale getirelim, 

$$
\left[\begin{array}{ccc}
1 & 1 & 6 \\
3 & 0 & 1 \\
1 & 1 & 4 \\
\end{array}\right]
\left[\begin{array}{ccc}
2 & 2  \\
5 & 5  \\
0 & 0  \\
\end{array}\right]
$$

Satýrsal bakýþa göre soldaki matrisinin herhangi bir $i$ satýrýndaki her
öge, saðdaki matrisin tüm satýrlarýný kombine ederek sonucun $i$ satýrýný
ortaya çýkarýr. Mesela en üst (birinci satýr) için

$$ 
1 \cdot
\left[\begin{array}{cc}
2 & 2
\end{array}\right] +
1 \cdot
\left[\begin{array}{cc}
2 & 2
\end{array}\right] + 
6 \cdot
\left[\begin{array}{cc}
0 & 0
\end{array}\right] 
=
\left[\begin{array}{cc}
7 & 7 \\
\vdots & \vdots \\
\vdots & \vdots
\end{array}\right] 
$$

Diðerleri için benzer iþlem uygulanýr, 

$$ 
3 \cdot
\left[\begin{array}{cc}
2 & 2
\end{array}\right] +
0 \cdot
\left[\begin{array}{cc}
2 & 2
\end{array}\right] + 
1 \cdot
\left[\begin{array}{cc}
0 & 0
\end{array}\right] 
=
\left[\begin{array}{cc}
7 & 7 \\
6 & 6 \\
\vdots & \vdots
\end{array}\right] 
$$

ve böyle devam edilir.

\end{document}
