\documentclass[12pt,fleqn]{article}\usepackage{../../common}
\begin{document}
Ders 6

Bu derste iþlenecek önemli konular þunlar, bir matrisin kolon uzayý
(columnspace), ki bu konuya önceki derste deðindik, diðeri ise bir
matrisin sýfýr uzayý (nullspace) ve alt uzaylar. 

Þimdi alt uzaylar hakkýnda bir düþünce deneyi yapalým. Diyelim ki elimde $P$
ve $L$ diye iki tane alt uzay var, $P$, $\mathbb{R}^3$ içinde bir düzlem
(plane), $L$ ise $P$ üzerinde olmayan bir çizgi (line). $P$ ve $L$ tabii ki
orijinden geçiyorlar, yoksa alt uzay olmazlar. 

\includegraphics[height=4cm]{6_01.png}

[Üstteki resimdeki yazý orijinden geçen düzlem altuzaydýr diyor]. Soru þudur; bu
iki uzayýn birleþimi (union) bir alt uzay oluþturur mu?  Cevap hayýr. Çünkü
belirtilen müstakbel uzay toplanabilme prensibine izin vermiyor. Bu prensibe
göre bir uzay içindeki iki vektörün toplamý ayný uzay içinde olmalýydý deðil mi?
Bir çizgi ve düzlemin toplamý olan ``þey'' bu tanýma uymuyor. Gayet rahat bir
þekilde düzlemden bir vektör, $L$'den bir vektör alýp onlarý toplarsam bambaþka
bir yöne iþaret eden bir yeni vektör elde edebilirim ve bu vektör $P \cup L$
içinde olmaz.

Peki $P \cap L$'i düþünelim, yani her iki uzayýn kesiþimi olan küme bir
alt uzayý mýdýr? Kesiþimde ne olduðunu düþünelim, sadece orijin noktasý
var, o zaman kümede sadece sýfýr noktasý var. Sýfýr vektörü tek baþýna bir
alt uzay oluþturabilir mi? Evet. Bu küme bir alt uzay oluþturabilir mi?
Cevap yine evet. 

Soruyu daha genelleþtirerek soralým: herhangi iki alt uzay $S,T$'yi alalým,
$S \cap T$ her zaman bir alt uzay olur mu? Bu alt uzay daha ufak olacaktýr,
çünkü þartlarý daha spesifik hale getirdik, bir nokta hem $S$ hem $T$
içinde olmalýdýr, herhalde bu þarta uyan noktalar daha azdýr. Ama cevap
yine evet. Düþünelim; kesiþimden $v,w$ adlý iki vektör alalým, o zaman $u +
w$, $S$ içinde mi? Evet, çünkü $u,w$'nin ikisi de taným itibariyle $S$
içinde, ve $S$ bir alt uzay olduðuna göre onun içindeki iki vektörün
toplamý da ayný uzay içinde olmlaya meburdur, ve ayný durum $T$ için de
geçerlidir. Toplam demek ki hem $S$ hem $T$ içinde olacaktýr, bunu
söylerken yani ``hem $S$ hem $T$ içinde'' derken ayný zamanda kesiþimi
tarif etmiþ olmadýk mý? Evet, o zaman toplam ayný zamanda kesiþim içindedir
de. Ýspat tamamlandý.

Alt uzay kontrolünün ikinci öðesi neydi? Sabitle çarpým yine uzay içinde
olmalý. Ayný argümaný kullanýyoruz, $S \cap T$ içinde olan bir vektörü
alýrým, mesela 7 ile çarparým, sonuç yine $S$ içinde çünkü $S$ bir alt
uzaydý, $T$ içinde, ayný sebepten dolayý. Demek ki çarpým hem $S$ hem $T$
içinde bu sebeple $S \cap T$ içinde.

O zaman iki alt uzayýn kesiþimi kesinlikle bir alt uzaydýr. 

$A$'nin Kolon Uzayý

$$ A = 
\left[\begin{array}{rrr}
1 & 1 & 2 \\
2 & 1 & 3 \\
3 & 1 & 4 \\
4 & 1 & 5 
\end{array}\right]
 $$

Bu matrisin kolon uzayý $\mathbb{R}^4$'un bir alt uzayýdýr çünkü kolondaki
vektörler 4 boyutlu. $A$'nin kolon uzayýnda, ki buna $C(A)$ diyelim, ne
vardýr? Üstte gördüðümüz $A$'nin tüm kolonlarý, üçü de, oradadýr. Ama bu
yeterli deðil tabii ki, deðil mi? 3 tane vektör koyarak bir alt uzay elde
etmiþ olmam. Bu alt uzayý nasýl doldururum? Üstteki üç vektörün tüm lineer
kombinasyonlarýný alýrým ve alt uzaya koyarým. Bunu yaparsam bir vektör
uzayý elde etmiþ olabilirim fakat hala bir alt uzay elde etmiþ olmam. 

Üstteki üç vektörün tüm kombinasyonlarý tüm 4 boyutlu uzayý (yani
$\mathbb{R}^4$) doldurur mu? Cevap hayýr. Yani ispatsal olarak bunu
göstermemiþ olsak bile sezgimiz bunun olmayacaðý yönünde (ki bu sezgi
doðru). 

Fakat bu kombinasyonlar daha ufak bir uzay olacaðý kesin. Ne kadar ufak? 

Bu sorunun cevabýný vermek için lineer denklemler ile bir baðlantý kurmak
faydalý olacak. Sorular þunlar,

``$Ax = b$ denkleminin her $b$ için bir çözümü var mýdýr?''

Bir diðer baðlantýlý soru, ``hangi $b$'ler için çözüm vardýr?''

1'inci sorunun cevabý hayýr; çünkü elimde 4 tane denklem, ama 3 tane
bilinmeyen var, yani bazý $b$'ler için çözümün olmamasý gayet
normal. Üstteki örneði geniþletirsek,

$$ A = 
\left[\begin{array}{rrr}
1 & 1 & 2 \\
2 & 1 & 3 \\
3 & 1 & 4 \\
4 & 1 & 5 
\end{array}\right]
\left[\begin{array}{r}
x_1  \\
x_2  \\
x_3  
\end{array}\right] 
=
\left[\begin{array}{r}
b_1  \\
b_2  \\
b_3  \\
b_4  
\end{array}\right]
 $$

Fakat ``bazý'' (eþitliðin) sað taraflarý için bu denklemin çözümü
vardýr. Bu da 2. sorunun cevabýný verecek, ilgilendiðim sað taraflar
bunlar. 

En bariz $b$ hangisi? Sýfýr vektör. O zaman $x$'i de sýfýr vektör alýrým,
ve bu bir çözüm olur. Bir diðer çözüm? [Sýnýftan bir cevap geliyor, hoca
yazýyor]


$$ A = 
\left[\begin{array}{rrr}
1 & 1 & 2 \\
2 & 1 & 3 \\
3 & 1 & 4 \\
4 & 1 & 5 
\end{array}\right]
\left[\begin{array}{r}
x_1  \\
x_2  \\
x_3  
\end{array}\right] 
=
\left[\begin{array}{r}
1  \\
2  \\
3  \\
4  
\end{array}\right]
 $$

Bu $b$ için çözüm vardýr? Hangi $x$ bu denklemi çözer? Haldir huldür hücre
hücre çarpýmý yapmaya gerek yok. Çarpýma kolon bakýþýný kullanýrsak, çözüm
basit, zaten $b$, $A$'nin birinci kolonu ile ayný. O zaman 1. kolondan 1
tane, 2. ve 3. kolondan 0 tane almak bize çözümü verir, yani

$$ A = 
\left[\begin{array}{rrr}
1 & 1 & 2 \\
2 & 1 & 3 \\
3 & 1 & 4 \\
4 & 1 & 5 
\end{array}\right]
\left[\begin{array}{r}
1  \\
0  \\
0  
\end{array}\right] 
=
\left[\begin{array}{r}
1  \\
2  \\
3  \\
4  
\end{array}\right]
 $$


Bir baþka çözüm, mesela $A$'nin ikinci kolonu $b$ olsaydý, o zaman 
$x = \left[\begin{array}{rrr} 0 & 1 & 0 \end{array}\right]^T$ bir çözümdür,
vs.. Tabii $b$ bulmanýn daha basit yolu, herhangi bir $x$ seçerim, çarpýmý
yaparým, $b$ elde ederim. Bunu yaparken $A$'nin kolonlarýnýn lineer
kombinasyonlarýný almýþ olmuyor muyum? Evet. Ve nihayet 2. sorunun cevabýna 
geldik; $Ax=b$'yi sadece eðer $b$, $A$'nin kolon uzayýnda ise çözebilirim. 

Niye olduðunun üzerinden bir daha geçelim. $A$'nin kolon uzayý taným
itibariyle tüm mümkün $Ax$'leri taþýmak mecburiyetindedir, yani mümkün tüm
$x$'ler $A$'yi çarpar, yani ``onun kolonlarýný kombine eder'', ve bu kolon
uzayýný oluþturur. 

Þimdi kolon uzaylarýyla yakýndan alakalý yeni bir soru daha soralým;
$A$'nin tüm kolonlarý birbirinden baðýmsýz midir (independent)? Yani
$A$'nin her kolonu kolon uzayýna ``yeni bir þeyler ekler mi?''. Bu ifadeyle
þunu kastediyoruz, eðer 3. kolon 1. ve 2.'nin kombinasyonu ise, 3. kolon
uzaya bir yenilik katmýyor demektir, çünkü 3. kolonu kullanmak yerine 1. ve
2.'yi kullanmak yeterli olacaktýr. 

$A$'ya dikkatle bakalým, $A$'nin herhangi bir kolonunu atsak, yine ayný
kolon uzayýný elde eder miyiz? $A$'nin baðýmsýz olmayan, diðerlerinin
kombinasyonu olan kolonu var mýdýr? Evet. 

$$ A = 
\left[\begin{array}{rrr}
1 & 1 & 2 \\
2 & 1 & 3 \\
3 & 1 & 4 \\
4 & 1 & 5 
\end{array}\right]
 $$

3'üncü kolon bariz bir þekilde 1. ve 2.'nin toplamý. Demek ki 1. ve 2. kolon
$A$'nin pivot kolonlarý, 3. deðil. Fakat bir dakika, 3. kolonu kötü çocuk
ilan etmeden, bakalým acaba 1. kolonu atsak, yine ayný kolon uzayýný elde
eder miydik? Cevap yine evet, çünkü 1. kolon 3.'nun 2.'den çýkartýlmýþ hali
de denebilir! O zaman 1. kolon atýlýp 2. ve 3. baðýmsýz olarak
tutulabilirdi. Fakat ben prensip / alýþkanlýk baðlamýnda  soldan
saða giderim, baðýmsýzlýðý solda ilk baktýðým kolonlara göre
irdelemeye uðraþýrým.

Sonuç olarak, $A$'nin kolon uzayý $\mathbb{R}^4$'un iki boyutlu bir alt
uzayýdýr. Ýki boyutlu çünkü baðýmsýz sadece iki kolon (vektör) var, bu iki
vektör sadece bir iki boyutlu düzlemi (plane) tanýmlayabilir. 

Sýfýr Uzayý

Tamamen farklý türde bir vektör /alt uzaydan bahsetmek istiyorum þimdi; sýfýr
uzayý. Mesela $A$'nin sýfýr uzayýnda ne vardýr? 

Bu uzayda $A$'nin kolon kombinasyonlarý yoktur. Farklý bir þekilde, $Ax$
denklemi baðlamýnda, $x$'ler vardýr. Hangi $x$'ler? $Ax=0$ denkleminin çözüm
olabilecek tüm $x$'ler. Yani,

$$ A = 
\left[\begin{array}{rrr}
1 & 1 & 2 \\
2 & 1 & 3 \\
3 & 1 & 4 \\
4 & 1 & 5 
\end{array}\right]
\left[\begin{array}{r}
x_1  \\
x_2  \\
x_3  
\end{array}\right] 
=
\left[\begin{array}{r}
0  \\
0  \\
0  \\
0  
\end{array}\right]
 $$

$x$'lerin 3 öðesi var, o zaman $A$'nin sýfýr uzayý $\mathbb{R}^3$'un bir
alt uzayý (tabii önce bu uzayýn düzgün bir alt uzay olduðunu da
ispatlamamýz gerekir, ve bunu göstereceðiz). 

$A$'ya çýplak gözle bakarak bu sýfýr uzayýný bulabilir miyiz? Her veri
üzerinde iþleyecek teknik tabii ki eliminasyon yapmak, vs. ve sonucu böyle
bulmak. Fakat bu basit örnek üzerinde sonucu hemen bulabilir miyiz? 

Ýlk çözüm, $x=0$. Bu bariz olan ilk çözüm. Sýfýr uzayýnda sýfýr vektörü
var, ki sýfýr uzayý bir vektör uzayý olma þansý artmýþ oldu böylece (çünkü
tüm vektör uzaylarýnda sýfýr vektörü olmalý). 

Bir diðer çözüm? Aradýðým $A$'nin kolon kombinasyonlarý, öyle ki o
kombinasyon sýfýr vektörü olsun. Bir tanesi,

$$ A = 
\left[\begin{array}{rrr}
1 & 1 & 2 \\
2 & 1 & 3 \\
3 & 1 & 4 \\
4 & 1 & 5 
\end{array}\right]
\left[\begin{array}{r}
1  \\
1  \\
-1  
\end{array}\right] 
=
\left[\begin{array}{r}
0  \\
0  \\
0  \\
0  
\end{array}\right]
 $$

Bir baþkasý? $\left[\begin{array}{rrr}2 & 2 & -2\end{array}\right]^T$,
yani bir önceki çözümün iki katý. Bir kalýp ortaya çýkmaya baþladý; Tüm
çözümler neye benzer? $c \cdot \left[\begin{array}{rrr}1 & 1 &
-1\end{array}\right]^T$, ki sabit $c=7$, olabilir, $c=11$
olabilir.. Tüm bu sýfýr uzayýnýn çizsem neye benzer? Bir çizgi olmaz mý?
Evet. Bu çizgi de sýfýrdan geçer zaten,

\includegraphics[height=4cm]{6_02.png}

[Yeþil ok $\left[\begin{array}{rrr}1 & 1 & -1\end{array}\right]^T$ ve tüm
çizgi onun katlarý]. Yani sýfýr uzayý $\mathbb{R}^3$'de bir çizgidir. 

Peki bu sýfýr uzayý bir alt uzay midir? Bir uzaya ``uzay'' demek sadece
onun belli þartlara uymasý ile mümkündür. Bu þartlar var mý ona bakalým.

$Av=0$ ise ve $Aw=0$ ise, o zaman $A(v+w)=0$ olmalý, yani $v$ sýfýr
uzayýnda ise, $w$ sýfýr uzayýnda ise, $v+w$ de sýfýr uzayýnda olmalý. Ýspat
son derece basit, 

$$ A(v+w) = 0 $$

$$ Av + Aw = 0 $$

ki $Av=0$ ve $Aw=0$ olduðunu biliyorum, o zaman 

$$ \cancelto{0}{Av} + \cancelto{0}{Aw} = 0 + 0 = 0 $$

Bu ispat tamam. Þimdi 2. kontrol; sabitle çarpým sýfýr uzayýnda mýdýr? 

$$ Ax = 0  $$

$$ A(c \cdot x) = 0  = c \cdot Ax = 0 = c \cdot \cancelto{0}{Ax} = 0$$

Bu þart da yerine getirildi. 

Þimdi, karþýlaþtýrma amaçlý olarak, sýfýr uzayý olmayan bir örneðe bakalým,

$$ A = 
\left[\begin{array}{rrr}
1 & 1 & 2 \\
2 & 1 & 3 \\
3 & 1 & 4 \\
4 & 1 & 5 
\end{array}\right]
\left[\begin{array}{r}
x_1  \\
x_2  \\
x_3  
\end{array}\right] 
=
\left[\begin{array}{r}
1  \\
2  \\
3  \\
4  
\end{array}\right]
 $$

Bunu çözen $x$'ler bir vektör uzayý oluþtururlar mý? Cevap hayýr. Bunu
görmenin en basit yolu, sýfýr vektörünün çözüm olmadýðýnýn
görmektir. Sýfýr vektörü yoksa orijin yok demektir, orijin yoksa bir uzay
elde edemeyiz. 

Bir çözüme bakalým, $x = \left[\begin{array}{rrr} 1&0&0 \end{array}\right]^T$. 
Diðeri?  $x = \left[\begin{array}{rrr} 0&-1&1 \end{array}\right]^T$

Böyle bir sürü çözüm var, fakat bunlar bir alt uzay deðil.. Büyük bir
ihtimalle bu çözümler bir çizgi ya da düzlem oluþturuyorlar, ama orijinden 
geçmiyorlar. 


\end{document}
