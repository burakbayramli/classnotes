\documentclass[12pt,fleqn]{article}\usepackage{../../common}
\begin{document}
Ders 9

Bu derste lineer bir vektörler kümesinin baðýmsýzlýk, ya da baðýmlýlýk
durumuna bakacaðýz. Bir vektör uzayý, ya da altuzay için baz (basis) nedir,
merkezi konumuz bu. Bir altuzayýn boyutu ne anlama gelir? Bu kelimelere net
anlamlar yükleyeceðimiz kritik ders bu olacak. Odaðýmýzýn ne olduðuna
dikkat, bir matrisin deðil bir vektör kümesinin baðýmsýzlýðýndan, bir
vektör kümesinin bir uzayý kapsamasýndan (span) bahsediyor olacaðýz. 

Daha önce vurgulamadýðým önemli bir bilgiyi hemen gündeme getirmek
istiyorum; diyelim ki elimde $m \times n$ boyutlarýnda $A$ matrisi var, ve
$Ax=0$ durumunu inceliyorum, ve $m < n$, yani $m$ tane formül var, ama
ondan daha fazla bilinmeyen var. 

Bu durumda $A$'nin sýfýr uzayýnda birþeyler vardýr (sýfýr vektör haricinde
tabii). Niye? Sebebini biliyoruz, zaten kabaca düþününce formülden fazla
bilinmeyen olunca bazý çözümler olmasý normal. Daha detaylý olarak, daha
önce bu iþin algoritmasýný gördük, elimde kesin serbest deðiþkenler olacak,
ve bu deðiþkenlere istediðim deðerleri atayacaðým, sonra geriye koyma ile
bazý baþka deðerler bulacaðým, vs. Yani sýfýr olmayan sonuçlar elde
edebileceðim. 

Bu matris durumu idi. Þimdi baðýmsýzlýk konusuna gelelim. 

Baðýmsýzlýk

Vektör $x_1,x_2,..,x_n$ lineer olarak baðýmsýzdýr (ileride lineer
kelimesini kullanmayacaðým, direk baðýmsýz diyeceðim), eðer bu vektörlerin
hiçbir kombinasyonu, ki kombinasyon $c_1x_1+...+c_nx_n$ olarak
tanýmlanabilir, sýfýr sonucunu {\em vermiyor} ise. Burada sýfýr
kombinasyonunu ayrý tutuyorum, yani $c_1=..c_n=0$ durumu), sýfýr
kombinasyonu, yani her vektörün sýfýr ile çarpýldýðý durum üstteki tarife
dahil deðil. Bu durum tatmin edilmiþ ise, o zaman $x_1,..,x_n$
baðýmsýzdýr. Eðer tek bir tane bile sýfýr sonucunu verecek kombinasyon var
ise, o zaman bu vektörler baðýmlýdýr.

Örnek

\includegraphics[height=4cm]{9_01.png}

Diyelim ki arka arkaya duran iki vektör var, biri diðerinin iki katý, ve
ayný yönü gösteriyorlar [hocanýn çizimi tam olmadý ama ayný yöndeler
:)]. Bu vektörler baðýmsýz mýdýr? Hayýr. Çünkü sýfýr elde etmek kolay,
$2v_1 - v_2 = 0$. Yani sýfýr sonucunu elde edebilecek bir kombinasyon
mevcut. 

Peki alttaki durumda?

\includegraphics[height=4cm]{9_02.png}

Ki $v_2$ sýfýr vektör. Hangi $c_1,c_2$ için $c_1v_1+c_2v_2$ sýfýr sonucunu
alýrým? Eðer $c_1=0,c_2=6$ dersem sýfýr sonucu gelir, tabii $c_1=0$
dedikten sonra $c_2$ ne seçersem seçeyim sýfýr sonucu olurdu zaten. Yani
sýfýr vektörü iþin içine dahil ise, ortada baðýmsýzlýk kalmaz, sýfýr
sonucunu verecek ve kendisi sýfýr olmayan bir kombinasyon muhakkak
bulunur. 

Ya alttaki durum? 

\includegraphics[height=4cm]{9_03.png}

Geliþigüzel üç tane vektör çizdim. Cevap baðýmlý, nereden biliyorum? Çünkü
üstte belirttiðim  $m < n$ durumu sayesinde. Baðlantý nerede? $A$'yi þöyle
yazarsam, 

$$ 
A = 
\left[\begin{array}{rrr}
\uparrow & \uparrow & \uparrow \\
v_1 & v_2 & v_3 \\
\downarrow & \downarrow & \downarrow 
\end{array}\right]
$$

ya da resme bakarak kabaca bazý büyüklükler yazarsam mesela, 

$$ 
A = 
\left[\begin{array}{rrr}
2 & 1 & 2.5 \\
1 & 2 & -1 \\
\end{array}\right]
$$

Bu matrisin boyutlarý $2 \times 3$. Elimizde bazý serbest deðiþkenler
olacak, ve 

$$ 
A = 
\left[\begin{array}{rrr}
2 & 1 & 2.5 \\
1 & 2 & -1 \\
\end{array}\right]
\left[\begin{array}{r}
c_1 \\ c_2 \\ c_3
\end{array}\right] = 0
$$

durumunda $c_1,c_2,c_3$ için bir çözüm olacak. $A$'nin kolonlarý baðýmlý,
çünkü onlarý belli þekillerde kombine ederek sýfýr sonucunu elde
edebiliyorum. 

Yani eðer $n$ boyutunda isem baðýmsýzlýk sorusunu vektörleri matrisin
kolonlarýna yerleþtirerek direk cevaplandýrabilirim. Vektörler baðýmsýzdýr
eðer bu vektörlerin yerleþtirildiði $A$'nin sýfýr uzayýnda sadece sýfýr
vektörü var ise. Eðer yani sýfýr uzayýnda sýfýr vektörü haricinde baþka
vektörler var ise, o zaman vektörler baðýmlýdýr. 

Üsttekini kerteler üzerinden ifade edelim; eðer kerte $r = n$ ise
baðýmsýzlýk vardýr, çünkü tüm kolonlarda bir pivot olur. Baðýmlýlýk
durumunda $r < n$, serbest deðiþkenler var, diðer durumda yok. 

Kapsamak (Span) 

Eðer bir uzay $v_1,...,v_n$ vektörlerinin tüm kombinasyonlarýndan oluþuyor
ise,  bu vektörlerin bu altuzayý kapsadýðý söylenir.

Elimde birkaç vektör varsa, $S$ diye bir uzayý kapsadýklarýný
söyleyebilirim, ki $S$ bu vektörlerin tüm kombinasyonlarýný içerir, bu $S$
uzayý bu vektörleri içinde tutabilecek en ufak uzay olacaktýr. Bu
vektörleri içeren baþka uzaylar da olabilir, ki taným itibariyle bu uzaylar
ayný þekilde, en azýndan, tüm kombinasyonlarý içermelidir (tabii daha
fazlasýna da sahip olabilirler). Ama sadece tüm kombinasyonlarý içeren uzay
en minimal ve kapsanan uzaydýr. 

Kolon uzayýna gelelim: bir matrisin kolonlarýný alýp tüm kombinasyonlarýný
hesaplarsam kolon uzayýný elde ederim. Bu kolonlar kolon uzayýný
kapsarlar. 

Peki kolonlar baðýmsýz mýdýr? Belki evet, belki hayýr, bu kolonlarýn ne
olduðuna baðlý. Bana en ilginç gelen durum baðýmsýz olduklarý durum, çünkü
bu durumda tam kararýnda vektöre sahibim demektir. Daha fazlasý baðýmlýlýk
olur, daha azý, kolon uzayýný oluþturamam. Tam kararýnda olduðu durum
eldeki vektörlerin bir baz oluþturduðu durumdur. Ýþte ``baz'' kelimesi
burada devreye girdi. 

Taným

Bir vektör uzayýnýn bazý $v_1,...,v_d$ gibi bir vektör dizisidir, ki bu
dizinin iki özelliði vardýr

1) Vektörler baðýmsýzdýr

2) Vektörler tüm uzayý kapsarlar

Bir altuzayý tarif etmek için mesela bana onun bazýný verirseniz, bu uzayý
tamamen tarif etmiþ olursunuz. 

Ornek

Uzay $\mathbb{R}^3$ olsun. Bana bu uzay için bir baz verin. Bu bir vektör
listesi olacak, ve bu vektörler tam kararýnda olmalý. Bir baz þu olabilir, 

$$ 
\left[\begin{array}{r}
1 \\ 0 \\ 0 
\end{array}\right],
\left[\begin{array}{r}
0 \\ 1 \\ 0 
\end{array}\right],
\left[\begin{array}{r}
0 \\ 0 \\ 1 
\end{array}\right]
 $$

Tek baz deðil, dikkat. Ama bu bir baz. Vektörler baðýmsýz mý? Evet. Yani,
nihayetinde üstteki vektörler $x,y,z$ eksenlerini temsil ediyorlar, ve bu
eksenler birbirinden baðýmsýz olmasa baþýmýz dertte demektir :) Baðýmsýzlýk
testi neydi, üstteki vektörler $c_1,c_2,c_3$ ile çarpýp toplasam ve sýfýr
elde edersem, $c_1=0,c_2=0,c_3=0$ demektir, baþka hiçbir kombinasyon bunu
baþaramaz.  

Matris dilinde konuþmak istersek, üstteki vektörleri bir matrisin kolonlarý
yapabilirdim, ve bu matris tanýdýðýmýz, bildiðimiz birim matrisi olurdu. O
zaman ``birim matrisinin sýfýr uzayý nedir?'' diye bir soru sorabilirdim,
cevap ``sadece sýfýr vektörü'' olurdu. Bunu duyunca ben de ``tamam, o zaman
kolonlar birbirinden baðýmsýz'' derdim. 

Baþka bir baz bulabilir miyim? Diyelim ki [hoca kafadan atarak iki vektör
yazýyor, ama herhalde zihninin bir köþesinde bu vektörlerin birbirinin katý
olmamasýna dikkat ediyordur],

$$ 
\left[\begin{array}{r}
1 \\ 1 \\ 2 
\end{array}\right],
\left[\begin{array}{r}
2 \\ 2 \\ 5 
\end{array}\right]
 $$

Tahminimiz ``hayýr'' deðil mi? Herhalde hafiften seziyoruz ki
$\mathbb{R}^3$'te bazý vektörler vardýr ki üstteki iki vektörün kombinasyonu
olarak temsil edilemezler. O zaman bir vektör daha eklemek lazým, çünkü
üstteki iki vektör $\mathbb{R}^3$'u kapsamýyor. Hangi vektörü eklemeliyim?
Eðer $\left[\begin{array}{rrr}3&3&7\end{array}\right]^T$ eklersem bu kötü
bir seçim olurdu, çünkü bu vektör üstteki iki vektörün toplamýdýr, yani bu
yeni vektör {\em baðýmlý} olur, diðer iki vektör ile ayný düzlemde olur. 

O zaman hangi vektörü seçmeliyim? Üstte tarif ettiðim düzlemde olmayan
herhangi bir vektörü seçebilirim. Bir tane seçiyorum; hepsi bir arada

$$ 
\left[\begin{array}{r}
1 \\ 1 \\ 2 
\end{array}\right],
\left[\begin{array}{r}
2 \\ 2 \\ 5 
\end{array}\right],
\left[\begin{array}{r}
3 \\ 4 \\ 8
\end{array}\right]
 $$

Galiba bu oldu, en azýndan 3. vektör diðer ikisinin toplamý deðil [hoca
burada bir hata yapmýþtý, bir sonraki derste düzeltti, buraya düzeltilmiþ
hali koyduk]. Peki bu vektörlerin baz olup olmadýðýnýn kesin testi nedir?
Onlarý bir matrisin kolonlarýna koyarsýnýz, eliminasyon uygularsýnýz,
serbest deðiþken ortaya çýkýyor mu bakarsýnýz, ya da tüm kolonlar pivot
kolonlarý mýdýr, ona bakarsýnýz. Gerçi elimizde bir kare matris olur, o
zaman en basit kontrol matrisin tersi alýnabilir olup olmadýðý.

Þimdi iki vektör durumuna dönelim,

$$ 
\left[\begin{array}{r}
1 \\ 1 \\ 2 
\end{array}\right],
\left[\begin{array}{r}
2 \\ 2 \\ 5 
\end{array}\right]
 $$

Bu iki vektör herhangi bir uzay için baz oluþturur mu? 

Tabii ki, bu mümkün olabilir. Bu iki vektör baðýmsýz. Kabaca çizersem onlarý,

\includegraphics[height=4cm]{9_04.png}

Bu iki vektörün tüm kombinasyonlarý bir düzlemi kapsayabilir, bu uzay
$\mathbb{R}^3$'un tamamý olmayabilir, ama üstte görüldüðü gibi onun
içindeki bir düzlem olabilir [belki çizimden anlaþýlmýyor, iki vektör
düzlemin tam üzerinde, yani içinde]. 

Kötü seçim olan $\left[\begin{array}{rrr}3&3&7\end{array}\right]^T$'i
eklersem, bu üç vektör yine üstteki düzlemi kapsar, ama o zaman eldeki
vektörler bir baz olmaz, çünkü ilk iki vektörü kullanarak 3. vektörü
oluþturabilirim. 

Neler öðrendik? Baz özgün (unique) deðil. Ayný uzayý tarif eden pek çok baz
olabilir. Fakat bu bazlarýn bir ortak noktasý var. Herhalde ne
söyleyeceðimi hafiften tahmin ediyorsunuz, zaten üstte 3 vektörden 2
vektöre geri dönünce bu iki vektörün bir baz olmayacaðýný
hissetmiþtiniz. Niye? Çünkü sayýlarý yeterli deðil. 3 boyutlu bir uzay olan
$\mathbb{R}^3$'i kapsamak için 3 tane vektör gerekir. Eðer uzay
$\mathbb{R}^n$ olsaydý baz için gerekli vektör sayýsý $n$ olurdu. 

Kural 

Verilen bir uzay için, ki bu kolon uzayý, bir sýfýr uzayý, vs. bile
olabilir, bu uzay için bulunacak her baz içinde ayný sayýda vektör
olmalýdýr. 

$S_1$ uzayýnýn bir bazýnda 6 vektör var ise, bir diðeri içinde de 6 vektör
olmalýdýr. Bu 6 sayýsý bana bu uzayýn ne kadar büyük olduðunu da söylüyor
bir bakýma. Ve tabii kontrol için de bu iyi, baþka bir yönden 7 tane vektör
bulmuþsam 1 tane fazla bulmuþum, 5 tane ise bir tane eksik. 

Bu sayý nedir? Bugünün son tanýmýna geldik böylece, bu sayý o uzayýn
{\em boyutudur}. 

Örnek

Uzay $C(A)$, yani $A$'nin kolon uzayý. $A$ þöyle,

$$ 
\left[\begin{array}{rrrr}
1 & 2 & 3 & 1 \\
1 & 1 & 2 & 1 \\
1 & 2 & 3 & 1
\end{array}\right]
 $$

Üstteki matrisin kolonlarý $A$'nin kolon uzayýný kapsar mý? Evet, çünkü
zaten kolon uzayýnýn tanýmý budur. Peki üstteki kolonlar bir baz oluþturur
mu? Bu kolonlar baðýmsýz mý? Hayýr. Sýfýr uzayýnda sýfýr olmayan vektörler
olacak. Mesela? Kolonlara bakýyorum, 3. kolon 1. ve 2. kolonun toplamý, o
zaman 

$$ 
\left[\begin{array}{r}
-1 \\ -1 \\ 1 \\ 0
\end{array}\right]
 $$

vektörü sýfýr sonucunu verir. Yani baðýmsýzlýk yok, baz üstteki 4 vektör
deðil. Peki hangi vektörler üstteki matrisin kolon uzayý için bir baz?
Ödevlerde, sýnavlarda bu soru olarak çýkacak, ``vs. matrisinin kolon uzayýnýn
bazýný bul''. Tabii ki pek çok farklý cevap olabilir, en doðal olan cevap
nedir? 

Doðal cevap pivotlarýn olduðu kolonlardýr. Eðer bu matris üzerinde
eliminasyon yapsam iki tane pivot çýkartabilirdim sadece. Pivot sayýsý ayný
zamanda matrisin kertesidir. Eðer pivotlar bir baz oluþturuyorsa o zaman
þunu da söyleyebilirim, pivot sayýsý $A$'nin kolon uzayýnýn boyutudur. 

Dilin kullanýmýna dikkat: $A$'nin boyutu demiyorum, ya da $A$'nin sýfýr
uzayýnýn boyutu demiyorum, $A$'nin kolon uzayýnýn boyutu diyorum. Ayný
þekilde, bir altuzayýn kertesinden bahsetmiyorum, bu yanlýþ olurdu, $A$'nin
kertesinden bahsediyorum. $A$'nin kertesi ayný zamanda kolon uzayýnýn
boyutu ile ayný. Terimleri doðru yerlerde kullanmak lazým. 

Peki üstteki matrisin kolon uzayýnýn baþka bir bazý ne olabilir? Eh, 1. ve
2. kolonlarý almýþtýk önce, niye 1. ve 3. kolonlar olmasýn? Ya da 2. ve 3,
ya da 2. ve 4.. Matrisin kolonlarý dýþýnda bir baz da olabilir bu
arada. Mesela, 

$$ 
\left[\begin{array}{r}
2 \\ 2 \\ 2 
\end{array}\right],
\left[\begin{array}{r}
7 \\ 5 \\ 7 
\end{array}\right]
 $$

1. vektörün nereden geldiði bariz, 2. vektör $A$'nin tüm kolonlarýnýn
toplamý. Vektörler baðýmsýz, ve sayý doðru, iki tane vektör. 

Sýfýr uzayýna gelelim: $A$'nin sýfýr uzayýnýn boyutu nedir? Mesela biraz
önce bulduðumuz vektör

$$ 
\left[\begin{array}{r}
-1 \\ -1 \\ 1 \\ 0
\end{array}\right]
 $$

bu uzayý kapsamak için yeterli mi? Hayýr. En azýndan bir tane daha vektöre
ihtiyacým var. Üstteki vektörün son iki hücresini serbest deðiþken denemesi
olarak kabul edersem, deðiþik serbest deðiþkenlere göre ilk iki hücre için
farklý deðerler hesaplayabilirim,

$$ 
\left[\begin{array}{r}
-1 \\ -1 \\ 1 \\ 0
\end{array}\right],
\left[\begin{array}{r}
-1 \\ 0 \\ 0 \\ 1
\end{array}\right]
 $$

Üstteki vektörler ayný zamanda iki özel çözüm deðil mi? Çünkü farklý
serbest deðiþken atamalarýyla onlarý elde ettim bir bakýma. Peki bu iki
vektör sýfýr uzayý için bir baz oluþturur mu? Evet. Ýspatýný vermiyoruz ama
cevap evet. Sýfýr uzayýnýn boyutu 2. Sýfýr uzayýnýn boyutu ayný zamanda
serbest deðiþken sayýsýna eþit. Ya da $n-r$'ye eþit. 

\end{document}


