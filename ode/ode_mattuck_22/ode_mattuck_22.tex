\documentclass[12pt,fleqn]{article}\usepackage{../../common}
\begin{document}
Ders 22

Mühendislerin Laplace Transformunu sevmesinin sebeplerinden biri
fonksiyonlarda kesintili / birdenbire zýplayan geçiþleri (jump
discontinuities) durumlarýnda bozulmadan iþleyebiliyor olmasýdýr. 

Kesintili fonksiyonlardan biri birim adým fonksiyonudur. Fonksiyonun
kendisi tartýþma yaratan bir fonksiyon aslýnda, sýfýr noktasýnda hangi
deðere sahip olduðu hala kararlaþtýrýlamadý. Bazýlarý 0 diyor, bazýlarý 1
diyor, ben bu derste tanýmsýz býrakacaðým. Yani

\includegraphics[height=2cm]{22_1.png}

$u(t)$ birim adým

$u(0)$ tanýmsýz

Bazen sýfýr yerine baþka bir noktada zýplama olmasýný isteyebilirim. O
zaman fonksiyonu kaydýrmak mümkün, mesela $a$ kadar. 

\includegraphics[height=2cm]{22_2.png}

$$ u_a(t) = u(t-a) $$

Birim Kutu

\includegraphics[height=2cm]{22_3.png}

Kutuyu birim adýmlar kullanarak temsil edebilirsek iyi olur, çünkü
birim adýmlarýn Laplace transformunu biliyoruz. 

$$ u_{ab} := u_a(t) - u_b(t) $$

$$ = u(t-a) - u(t-b) $$

Mantýklý gözüküyor, $b$'ye gelene kadar birim adým gibi gidiyoruz, $b$'de
birim adýmý iptal edecek þekilde birim adýmý çýkartýyoruz. 

Peki bu fonksiyonlar niye faydalý? Diðer fonksiyonlarý çarptýklarýnda o
fonksiyonlarý faydalý þekillerde deðiþtirebildikleri için. Diyelim ki þöyle
bir $f(t)$ var.

\includegraphics[height=2cm]{22_4.png}

$$ u_{ab}f(t) $$

neye benzerdi? Eðer $a,b$ arasýnda $u_{ab}$ 1 deðerine eþitse, çarpým bu
aralýkta $f(t)$'yi olduðu gibi alýr. Diðer noktalarda sýfýr yapar. Alttaki
þekil ortaya çýkar (mavi çizgiler). 

\includegraphics[height=3cm]{22_5.png}

$f(t)$'nin bir parçasýný çekip çýkartýyoruz yani. Bu çok ise yarayan bir
numara / teknik. 

Birim adýmýn Laplace Transformunu alalým.

$$ \mathcal{L}(u(t)) = \int_{0}^{\infty} e^{-st} u(t) \ud t  $$

Entegral alt sýnýrý 0'dan baþlýyor. Birim adým 0'dan büyükse hep 1, o zaman
üstte $u(t)$'yi 1 alabiliriz. Yani üstteki aslýnda 1'in Laplace
Transformudur. 

$$ = \frac{1}{s} \ \ \ s>0 
\mlabel{1}
$$

Kaydýrýlmýþ birim adýmýn Laplace'i

$$
\mathcal{L} (u(t-a)) = 
\int_{a}^{\infty} 1 \cdot e^{-st} \ud t
$$

Alt sýnýr $a$ oldu çünkü $a$'dan önce sýfýr, sonrasý 1. Bizi tek
ilgilendiren kýsým fonksiyon 1 olduktan sonra, o zaman alt sýnýrý 
deðiþtiririz. 

$$ = \frac{e^{-st}}{-s} \bigg|_{a}^{\infty} =
\frac{e^{-as}}{s}
$$

Diðer bir özellik

$$ \mathcal{L}(e^{ct}f(t)) = F(s-c) $$

Ýspat: Eðer entegral temsilini açarsak

$$
= \int _{0}^{\infty} e^{ct}f(t)e^{-st} \ud t  = \int e^{(c-s)t} f(t) \ud t
$$

Bu son ifadeye baþka bir yönden eriþebilir miyiz? Mesela $F(s-c)$ nedir? 
$s$ yerine $s-c$ koyarsak (temel formülden türetelim)

$$ F(s-c) = \int _{0}^{\infty} f(t) e^{-(s-c)t} ft  $$

$$ = \int _{0}^{\infty} e^{(-s+c)t} \ud t  = \int _{0}^{\infty} e^{(c-s)t} \ud t $$

Ayný ifadeye eriþtik. 

(1) formülünde de ters yönde gidersek

$$ \mathcal{L}^{-1}(\frac{1}{s}) = ?  $$

Geriye giderken 1'e mi, birim adýma mý döneceðim? Þimdiye kadar geriye
giderken hep 1 seçtim. Artýk bu yeterli deðil. Problem nerede? Çünkü
Laplace Transformu entegral alt sýnýrý 0'dan baþlýyor, yani transform 0
öncesine bakmýyor bile. Bir fonksiyon $f(t)$ 0'dan önce envai türde deðere
sahip olabilir, Laplace Transformu ayný olacaktýr. 

\includegraphics[height=3cm]{22_6.png}

Eðer hangi tür problem ile uðraþtýðýmýzý biliyorsak bu durum bir sorun
çýkarmaz. Mühendisler þöyle kategorize ederler; eðer bir problem þimdiki
zamandan, $t=0$, baþlayýp ileri gidiyorsa o bir Laplace Transform
problemidir, eðer geçmiþ zaman verisi gerekiyorsa o bir Fourier Transform
problemidir. 

Eðer 1 noktasýndan ( $t=0$ ) geriye giderken ortaya çýkan tanýmsýzlýktan
(ambiguity) kurtulmak istiyorsak, ters Laplace'dan elde edilen fonksiyonu
birim adým ile çarparýz, böylece $t=0$ öncesi sýfýr olmasýný garanti etmiþ
oluruz.

$$ f(t) \leadsto F(s) $$

$$ \mathcal{L}^{-1}(F(s)) = u(t)f(t) $$

Böylece ters Laplace özgün (unique) hale gelir.

Taþýma (translation) ve Laplace 

$\mathcal{L}(f(t-a))$'yi $\mathcal{L}(f(t))$ temel alarak yazabilir miyiz?
Bu ne yazik ki mumkun degil. 

\includegraphics[height=3cm]{22_7.png}

Üstte $f(t)$'yi $a$ kadar saða kaydýrdýk. Kesik çizgili yeni, taþýnmýþ
fonksiyonu niye düz çizgili fonksiyon baðlamýnda temsil edemiyorum? Sebep
bariz, çünkü $f(t)$'nin sýfýr öncesi deðeri. O deðer Laplace Transformu
sýrasýnda kullanýlmaz, ama taþýndýktan sonra yeni fonksiyonda
kullanýlýr. Aradaki bu uyuþmazlýðý, eksikliði kapatmak mümkün deðildir. 

Yapabileceðimiz en iyi þey, kaybettiðimiz bölümü $u(t-a)f(t-a)$ kullanýmýyla
Laplace Transformundan silmektir. Altta köyü çizgili olan kýsmýn
transformunu alýrýz yani.

\includegraphics[height=2cm]{22_8.png}

$$ u(t-a)f(t-a) \leadsto e^{-as}F(s) 
\mlabel{2}
$$

(2) geçiþi Laplace Tablosunda görülebilir.

Fakat $f$ çoðunlukla $t-a$ formunda gelmez, mesela $\sin(t)$, $\cos(t)$
olur, o zaman þu teknik kullanýlabilir,

$$ u(t-a)f(t) \leadsto e^{-as}\mathcal{L}(f(t+a)) $$

Bu tekniðe bazýlarý üstel kaydýrma fonksiyonu (exponential shift formula)
diyor. Biz bu terimi kullanmayacaðýz. Biz $t$ ekseni taþýnmasý (t-axis
translation) formülü diyeceðiz. 

(2)'nin ispatý

$$ \int_{0}^{\infty}e^{-st}u(t-a)f(t-a) \ud t $$

Ama formülde $F(s)$ kullanmak istiyorum, o zaman $f(t-a)$'yi bir þekilde
$f(t)$ yapmam lazým. Deðiþken deðiþimi yaparým. 

$$ t_1 = t-a $$

$$ = \int_{a}^{\infty}e^{-s(t_1+a)}u(t_1)f(t_1) \ud t_1 $$

$$ = e^{-sa} \int_{-a}^{\infty}e^{-st_1}u(t_1)f(t_1) \ud t_1 $$

$u(t_1)$ $t_1<0$ için sýfýr olduðuna göre $-a,0$ arasýný entegral alt
sýnýrýndan atabiliriz, 

$$ = e^{-sa} \int_{0}^{\infty}e^{-st_1}f(t_1) \ud t_1 $$

$u(t_1)$'in kendisini niye attýk? Çünkü sýfýr sonrasý 1 deðerinde zaten. 

``Ama $t$ deðiþkeni $t_1$ haline geldi, artýk bu Laplace Transformu olmaz''
diye düþünenler olabilir. Fakat entegrasyonda kullanýlan fonksiyonun illa
$t$ ismini taþýmasý gerekmiyor, çünkü o deðiþken bir yer tutucu (dummy)
sadece. Entegre edilen, sýnýrlar düzgünse, üstteki de bir Laplace
Transformudur.

Yani

$$ u(t-a)f(t-a) \leadsto e^{-as}\mathcal{L}(f(t)) $$

$t$ yerine $t+a$ geçirdim.

Eðer $u(t-a)f(t)$ olsaydý?

$$ u(t-a)f(t-a+a) \leadsto e^{-as}\mathcal{L}(f(t+a))) $$

Örnek 

$$ u_{ab} = u(t-a) - u(t-b) $$

$$ \leadsto \frac{e^{-as}}{s} - \frac{e^{-bs}}{s} $$

Örnek

$$ u(t-1)t^2 $$

Transformu nedir? 

$$ \leadsto e^{-s}\mathcal{L}((t+1)^2) = 
e^{-s}\mathcal{L} (t^2+2t+1) =
e^{-s}(\frac{2}{s^3} + \frac{2}{s^2} + \frac{1}{s})
 $$

Bayaðý kalabalýk bir sonuç oldu, fakat transformunu aldýðýmýzda elde edilen
yukarýdaki fonksiyon da o kadar basit bir fonksiyon deðil zaten; altta
yeþil renkle iþaretli 

\includegraphics[height=2cm]{22_9.png}

Fonksiyon kesintili (discontinuous) bir fonksiyon. Kesintinin nerede
oluþtuðu $e^{-as}$ formülünde $a$, yani $e^{-s} = e^{-1\cdot s}$ için 1. 

$$ \mathcal{L}^{-1}\bigg( \frac{1+e^{-\pi s}}{s^2+1} \bigg) $$

Cevabýn bir kesikli fonksiyon olacaðýný þimdiden biliyoruz, çünkü formül
içinde üstel bir fonksiyon var. Ýlk yapacaðýmýz iþ üstel fonksiyonu yanlýz
kalacak þekilde ifadeyi parçalamak. 

$$ = \frac{1}{s^2+1} + \frac{e^{-\pi s}}{s^2 + 1}$$

Önce suna bakalým

$$ \frac{1}{s^2+1} \stackrel{-1}{\leadsto}  \ \ ? $$

Eðer üsttekine cevap olarak $\sin(t)$ dersek, yanlýþ olur. Eðer üstel
fonksiyon formülün geri kalanýnda olmasaydý, o zaman $\sin(t)$ olabilirdi,
ama þimdi 

$$ \frac{1}{s^2+1} \stackrel{-1}{\leadsto} u(t)\sin(t) $$

kullanmamýz lazým. Peki

$$ \frac{e^{-\pi s}}{s^2 + 1} \stackrel{-1}{\leadsto} \ \ ? $$

Yine (2)'yi kullanýrým, 

$$\stackrel{-1}{\leadsto} u(t-\pi)sin(t-\pi) $$

Nihai cevap nedir? Ýki parçayý toplarým

$$ \frac{1}{s^2+1} + \frac{e^{-\pi s}}{s^2 + 1} \stackrel{-1}{\leadsto} 
u(t)\sin(t) + u(t-\pi)\sin(t-\pi) 
$$

Bu cevap teknik olarak doðru, ama biraz masajlayýp onu daha düzgün bir hale
getirmek lazým [hatta hoca þakayla karýþýk bu halde býrakýrsanýz sýnavda
not kýrarým diyor]. 

O zaman her parçaya teker teker bakalým. Formülün $u(t)\sin(t)$ kýsmý
sadece $t \ge 0$ için anlamlý, çünkü ondan önce birim step fonksiyonu
sýfýr. $u(t-\pi)\sin(t-\pi) $ kýsmi ise $t \ge \pi$ için anlamlý. Elimizde
iki tane senaryo var aslýnda, o zaman bu fonksiyonu parçalý, senaryolu
olarak gösterebiliriz. 

$$ 
f(t) = 
\left\{ \begin{array}{ll}
sin(t) & 0 \le t \le \pi \\
sin(t) + (-sin(t)) & t \ge \pi
\end{array} \right.
 $$

Terim $-\sin(t)$, $\sin(t-\pi)$'dan geldi, çünkü $\sin$'i $\pi$ kadar yana
kaydýrýrsak (altta kesik çizgili) 

\includegraphics[height=2cm]{22_10.png}

elde edilen $-\sin(t)$'dir. Nihai formül

$$ 
f(t) = 
\left\{ \begin{array}{ll}
sin(t) & 0 \le t \le \pi \\
0 & t \ge \pi
\end{array} \right.
 $$



\end{document}
