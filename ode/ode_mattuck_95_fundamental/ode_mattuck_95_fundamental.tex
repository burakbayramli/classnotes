\documentclass[12pt,fleqn]{article}\usepackage{../../common}
\begin{document}
Calculus'un Temel Teoremi (The Fundamental Theorem of Calculus)

Ana teoriyi ispatlamadan önce iki diðer teoriden bahsetmemiz, ispatlamamýz
lazým. Bu teorilerden biri Geçiþ Deðeri Teorisi (Intermediate Value
Theorem) diðeri Tanýmlý Entegraller Ýçin Ortalama Deðer Teoremi (Mean Value
Theorem for Definite Integrals). Geçiþ Deðeri Teorisi basitçe þunu söyler

Teori

$[a,b]$ aralýðýnda sürekli bir fonksiyon $y=f(x)$, $f(a)$ ve $f(b)$
arasýndaki her deðeri muhakkak alýr. Bir diðer deðiþle, eðer $y_o$, $f(a)$
ve $f(b)$ arasýndaki bir deðer ise $[a,b]$ aralýðýndaki bir $c$ için
muhakkak $y_0 = f(c)$ olmalýdýr. 

Geometrik olarak bu teori $y$ eksenini $f(a)$ ve $f(b)$ arasýnda kesen
$y=y_0$ yatay çizgisinin $y=f(x)$ fonksiyonunu muhakkak, en az bir kez
keseceðidir. Grafik altta. 

\includegraphics[height=4cm]{calc_multi_app_05.png}

Sezgisel olarak bu anlamlý deðil mi? Eðer sürekli bir fonksiyon var ise,
$f(a)$'dan $f(b)$'ye giderken o aralýktaki her sayýya bir kez ``uðramaya''
mecburuz. Etraflarýndan dolaþmamýz mümkün deðil, çünkü kesintili bir
fonksiyon deðil, kesintisiz / sürekli bir fonksiyonumuz var. Bu teorinin
daha detaylý ispatý için [1]'e bakýlabilir. 

Maks-Min Eþitsizliði

Eðer $[a,b]$ aralýðýnda $f$, maksimum deðer $\max f$'e ve minimum deðer
$min \ f$'e sahipse, 

$$ \min f \cdot (b-a) \le \int_a^b f(x) \ud x \le \max f \cdot (b-a) $$

demektir. 

\includegraphics[height=4cm]{calc_multi_app_08.png}

Bu kural diyor ki $f$'in $[a,b]$ üzerindeki entegrali hiçbir zaman $f$'in
minimum'u çarpý $[a,b]$ aralýðýnýn uzunluðu'ndan küçük olamaz, ve $f$'in
maksimumu çarpý $[a,b]$ aralýðýnýn uzunluðu'ndan büyük olamaz. 

Ýspat

Eðer $(b-a)$'yi $ \sum_{k=1}^n \Delta x_k$ olarak görürsek

$$ \min \ f \cdot (b-a) = \min \ f \cdot \sum_{k=1}^n \Delta x_k $$
$$  = \sum_{k=1}^n \min \ f \cdot \Delta x_k  $$

$[a,b]$ aralýðýndaki herhangi bir deðer $c_k$ için

$$  \le \sum_{k=1}^n f(c_k) \cdot \Delta x_k  $$

Öyle deðil mi? $min \ f$ deðeri en küçük deðer ise, $[a,b]$ aralýðýndaki
herhangi bir nokta $c_k$'nin $f$ deðeri bu deðere ya eþit, ya da ondan
büyüktür. Yani $min \ f \le f(c_k)$. Devam edersek

$$  \le \sum_{k=1}^n \max f \cdot \Delta x_k  $$

Üstteki benzer mantýðý takip ediyor, bu sefer $f(c_k) \le \max f $. Son
ifadedeki $max$'i dýþarý alabiliriz. 

$$ = \max f \sum_{k=1}^n \cdot \Delta x_k  $$

$$ = \max f (b-a)  $$

Ortalama Deðer Teoremi

Eðer $f$ fonksiyonu $[a,b]$ arasýnda sürekli ise o zaman $[a,b]$ aralýðýnda
olan bir $c$ noktasýnda

$$ f(c) = \frac{1}{b-a}\int_a^b f(x) \ud x $$

eþitliði doðru olmalýdýr. Yani alttaki resimde sol grafikteki mavi alanýn
$b-a$ ile bölünerek elde edilen ortalama deðeri, $[a,b]$ aralýðýndaki bir
$c$ üzerinden $f(c)$'ye muhakkak eþittir. Ya da bir kenarý $f(c)$, diðeri
$b-a$ olan bir diktortgenin alaný (alt saðdaki resim), mavi alanýn
tamamýna eþit olacaktýr.

\includegraphics[height=4cm]{calc_multi_app_07.png}

Maks-Min Eþitsizliðinin iki tarafýný $b-a$'ya bölersek

$$ \min f  \le \frac{1}{b-a} \int_a^b f(x) \ud x \le \max f  $$

elde ederiz. Eðer Geçiþ Deðeri Teorisi doðruysa, $\min f$ ve $\max f$
arasýndaki tüm noktalar ziyaret edilmelidir. O zaman böyle bir $f(c)$
kesinlikle var demektir.

Calculus'un Temel Teoremi

Teori

Eðer $f$ fonksiyonu $[a,b]$ arasýnda sürekli ise o zaman 

$$ F(x) = \int_a^x f(t) \ud t  $$

fonksiyonu da $[a,b]$ arasýnda süreklidir, ve bu fonksiyonun türevi
$f(x)$'in kendisidir.

Yani

$$ F'(x) = \frac{d}{dx}\int_a^x f(t) \ud t = f(x)   $$

\includegraphics[height=3cm]{calc_multi_app_03.png}

Ýspat

Türevin tanýmýný direk $F(x)$ üzerinde uygulayalým, $[a,b]$ içinde olan $x$
ve $x+h$ aralýðýný alalým, ve

$$ \frac{F(x+h)-F(x)}{h} $$

bölümünün limitinin, $h \to 0$ iken, $f(x)$'e gittiðini göstermeye
çalýþalým. $F(x+h)$ ve $F(x)$ fonksiyonlarýný entegralleri üzerinden
tanýmlayalým. O zaman üstteki formülün bölüm kýsmý

$$ F(x+h) - F(x) = \int_a^{x+h} f(t) \ud t - \int_a^x f(t) \ud t  $$

Entegrallerin toplam kuralýna göre üstteki formülün sað tarafý 

$$ \int_x^{x+h} f(t) \ud t  $$

ifadesidir. O zaman bölümün tamamý

$$ \frac{F(x+h)-F(x)}{h} = \frac{1}{h} \int_x^{x+h} f(t) \ud t   $$

Ortalama Deðer Teoremine göre, üstteki eþitliðin saðýndaki ifadenin, $x$ ve
$x+h$ aralýðýnda $f$'in aldýðý deðerlerden birine aynen eþit olduðunu
biliyoruz. Yani o aralýktaki bir $c$ için

$$ \frac{1}{h} \int_x^{x+h} f(t) \ud t = f(c) $$

kesinlikle doðru olmalý. Þimdi, $h \to 0$ oldukça, $x+h$ mecburen $x$'e
yaklaþmak zorunda kalacaktýr, çünkü $c$, $x$ ile $x+h$ arasýnda sýkýþýp
kalmýþtýr. $f$ fonksiyonu $x$ noktasýnda sürekli olduðuna göre, o zaman
$f(c)$, $f(x)$'e yaklaþmalýdýr. 

$$ \lim_{h \to 0} f(c) = f(x) $$

Þimdi elimizdeki bu bilgiyle baþa dönersek, 

$$ \frac{dF}{dx} = \lim_{h \to 0} \frac{F(x+h)-F(x)}{h} $$

$$  = \lim_{h \to 0} \frac{1}{h} \int_x^{x+h} f(t) \ud t   $$

$$ = \lim_{h \to 0} f(c) $$

$$ = f(x) $$

Ek olarak ilgili bir teori daha gösterelim.

Cauchy Ortalama Deðer Teorisi (Cauchy Mean-value Theorem)

Teori þöyle

Eðer $f,g$ fonksiyonlarý $[a,b]$ aralýðýnda sürekli ise ve $g'(x) \ne 0$
farz edildiði durumda $[a,b]$ arasýnda öyle bir $c$ vardýr ki,

$$ \frac{f'(c)}{g'(c)} = \frac{f(b)-f(a)}{g(b)-g(a)} $$

ifadesi doðrudur. 

Ýspat

Þimdi daha önceden gördüðümüz Ortalama Deðer Teorisi'ni (Cauchy olmayan)
iki kere kullanacaðýz. Teoriyi önce $g(a) \ne g(b)$ olduðunu göstermek için
kullanacaðýz. Çünkü eðer bu doðru olsaydý, Ortalama Deðer Teorisi 

$$ g'(c) = \frac{g(b) - g(a)}{b-a} = 0$$

olurdu, ki bu $[a,b]$ arasýndaki bir $c$ için baþta yaptýðýmýz faraziyemiz
$g'(x) \ne 0$ ile ters düþerdi. 

Ýkinci kullaným: $F(x)$ adýnda, $f,g$ fonksiyonlarýný kullanan baþka bir
fonksiyon kurgulayalým.

$$ F(x) = f(x) - f(a) - \frac{f(b)-f(a) }{g(b)-g(a)}[g(x)-g(a)] $$

Bu fonksiyonun türevi, $f,g$'nin türevi alýnabildiði her yerde alýnabilir
olur. Ayrýca $F(b) = F(a) = 0$. $a,b$ deðerlerini yerine koyarsak bunu
görebiliriz, mesela $x=a$ için

$$ F(a) = \cancelto{0}{f(a) - f(a)} -
\frac{f(b)-f(a) }{g(b)-g(a)}
[\cancelto{0}{g(a)-g(a)}] 
$$

$$  = 0 - 0 = 0 $$

O zaman, $F(b) = F(a) = 0$'dan bir sonuca daha eriþiriz. Bir fonksiyon
$a,b$ uçlarýnda sýfýr ise, bu fonksiyon bir þekilde azalýp, çoðalýyor, ya
da çoðalýp azalýyor demektir, yani kesinlikle bir yerde tepe yapýyor
demektir. Tepe yapmanýn Calculus'taki tercümesi $[a,b]$ arasýndaki bir $c$
için $F'(c)=0$ olmasýdýr. O zaman üstteki $F(x)$'in türevini alýrsak, ve
$x=c$ dersek, 

$$ F'(c) = f'(c) - \frac{f(b)-f(a)}{g(b)-g(a)}[g'(c)] = 0$$

doðru olmalýdýr. Türev alýrken $f(a)$ yokoldu çünkü sabitti, büyük bölüm
yerinde kaldý çünkü tamamý $g(x)$ için katsayý. Eðer tekrar düzenlersek,
negatif terimi sola alýrsak, ve iki tarafý $g'(c)$'ye bölersek,

$$ \frac{f'(c)}{g'(c)} = \frac{f(b)-f(a)}{g(b)-g(a)} $$

ifadesini elde ederiz. Yani baþtaki teoriyi elde etmiþ oluruz. 

Ortalama Deðer Teorisini ilk kez kullanmamýzýn sebebi, üstteki bölenin sýfýr
olmamasýný istediðimiz içindi, çünkü sýfýrla bölüm tanýmsýzdýr. 

Kaynaklar

[1] Hass, {\em Thomas Calculus 11th Edition}

\end{document}

