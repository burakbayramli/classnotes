\documentclass[12pt,fleqn]{article}\usepackage{../../common}
\begin{document}
Ders 3

Birinci Derece Lineer ODE

ODE konusunda en önemli denklemler 1. derece lineer
ODE'lerdir. Matematiksel pek çok modelde sürekli ortaya çýkarlar, ve
analitik olarak çözülebilir haldedirler. Þuna benzerler:

$$ a(x)y' + b(x)y = c(x) $$

Niye lineer? Çünkü üstteki formül $y$ ve $y'$ baðlamýnda lineer, 
$ay_1 + by_2 = c$'de $y_1$ ve $y_2$'nin lineer olmasý gibi lineer.

Eðer $c = 0$ ise bu denkleme homojen denir. 

1'inci formül yaygýn form'dur, ama standart form deðildir. Standart lineer
form alttakine benzer. Ýlk katsayý (coefficient) 1 deðerinde olmalýdýr,
yaygýn formu standarta çevirmek için tüm denklem $a(x)$'e
bölünebilir. $b/a$ olunca harf deðiþtirilir, $p$ denir, diðerine $q$ denir.

$$ y' + p(x)y = q(x) $$

1'inci derece lineer ODE'lerin ortaya çýktýðý modeller ýsý konsantrasyon
modelleri, karýþým (mixing) modelleri, daha az önemliler radyoaktif çürüme,
faiz hesabý, bazý hareket modelleri, vs. Bugün kullanacaðýmýz model iþi
konsantrasyon modeli, ismini deðiþtirerek iletim (conduction) ve yayýlma
(diffusion) kelimelerini kullanacaðýz.

Aktarma modeli ile baþlayalým. 

Diyelim ki elimizde içinde þu olan bir kap var, ve bu kap içinde tam ortada
(bir þekilde) asýlý duran bir kutu (küp) var. Bu kutunun duvarlarý kýsmen
izole edilmiþ. Ýçerideki kutunun sýcaklýðý $T$ ve dýþ bölmenin sýcaklýðý
$T_e$, $t$ zaman. Bu durum için nasýl bir diferansiyel denklem kurarýz?

\includegraphics[height=4cm]{3_1.png}

Newton'un Soðuma Kanununu kullanýyoruz ve sýcaklýðýn aktarýlarak baþka bir
noktaya geçtiðini düþünüyoruz (sýcaklýk radyasyon üzerinden de seyahat
edebilir), ve þu modeli kuruyoruz.

$$ \frac{dT}{dt} = k(T_e - T) $$

$k > 0$ akýþkanlýk (conductivity), bir sabit.

$$ T(0) = T_0 $$

$T$'nin zamana göre bir fonksiyon olduðunu unutmayalým. Kýsa olsun diye
çoðu zaman $T(t)$ ibaresi kullanýlmýyor.

Yayýlma (diffusion) modeli nasýl olurdu? 

\includegraphics[height=4cm]{3_2.png}

Bu model formülsel olarak neredeyse ayný, diyelim ki bu sefer küp içinde
tuz var, konsantrasyonu $C$, dýþarýdaki tuz kontsantrasyonu $C_e$. Yayýlma
modelinde tuzluluk ``yayýlýyor'', yüksek konstrasyondan alçak olana doðru
gidiyor. Bu modelde tuzun yayýlma hýzý, konstantrasyonlar arasýndaki farka
baðlýdýr.

$$ \frac{dC}{dt} = k(C_e - C) $$

Isý aktarým formülü standart lineer formda deðil, bu deðiþimi yapalým, ki
en azýndan lineer olduðunu görmüþ olalým.

$$ \frac{dT}{dt} + kT = kT_e $$

Çözelim

$$ y' + p(x)y = q(x) $$

Bu formülü entegre edici faktör (integrating factor) $u(x)$ kullanarak
çözeceðiz.

$$ uy' + puy = qu $$

Burada yapmak istediðimiz öyle bir $u$ bulmak ki sað ve sol taraf
onunla çarpýlýnca sol taraf bir þeyin türevi haline gelsin:

$$ ( ... )' = ..  $$

Böylece $x$'e göre iki tarafýn entegralini alýnca türev iþlemi
kaybolur, daha temiz bir ifade geriye kalýr. 

Parantez içinde ulaþmaya çalýþacaðýmýz ifade neye benzemeli? Hoca
$(uy)'$ fikrini ortaya attý. Bunun niye mantýklý bir seçim olduðunu
görmek zor deðil, eðer $(uy)'$ üzerinde türevin çarpým kuralýný
kullanýrsak, açýlýmý zaten 

$$ uy' + u'y $$

olacaktýr, ki bu ifade $uy' + puy = qu$ formülünün sol tarafýna
yakýndýr. Tek bir farkla, $u'$ yerine elimizde $pu$ var. O zaman u
öyle seçilmeli ki $u' = pu$ olsun. 

$$ u' = pu $$

Deðiþkenleri ayýr

$$ \frac{du}{u} = p $$

Entegrali al

$$ \int \frac{du}{u}  = \int p $$

$$ ln (u) = \int p(x) \ud x $$

Buradan $u$'nun ne olacaðýný kestirebiliriz. Unutmayalým, tüm $u$'lara
ihtiyacým yok, üstteki ifadeyi tatmin eden herhangi bir $u$
olabilir. Üstteki formülün iki tarafýný $e$ bazýna tabi tutarsak

$$ u = e^{\int p(x) \ud x} $$

Ýþte entegre edici faktörü bulduk. Herhangi bir (arbitrary) sabite ihtiyaç
yok çünkü tek $u$ kullandýk.

Metotu tekrar baþtan gözen geçirelim. Elimizde þu var:

$$ y'+py = q $$

1. Eðer formül standart lineer formda deðilse o forma geçir, çünkü
entegre edici formülde $p$ var, eðer form doðru deðilse doðru $p$
gelmez, iþler sarpa sarar.

2. Entegre edici faktör $$ e^{\int p(x) \ud x} $$ bul.

3. ODE'nin iki tarafýný bu faktör ile çarp.

4. Entegre et

Örnek

$$ xy' - y= x^3 $$

Standart form

$$ y' - \frac{1}{x}y = x^2 $$

Entegre edici faktör

$$ e^{-ln(x)} $$

Bunu daha basitleþtirebiliriz,

$$ e^{-ln(x)} = (e^{ln(x)})^{-1} = x^{-1} = \frac{1}{x}  $$

Standart formun iki tarafýný bu faktör ile çarpalým

$$ \frac{1}{x}y' - \frac{1}{x^2}y = x $$

Eðer her þeyi doðru yaptýysak sol tarafý direk entegre
edebiliriz. Parantezli forma koyalým, ve parantez içinin hakikaten
üstteki sonucu verip vermediðine de dikkat edelim.

$$ (\frac{1}{x}y)' = x $$

Entegrali alýnca

$$ \frac{1}{x}y = \frac{x^2}{2} + C $$

$$ y = \frac{x^3}{2} + C $$

Sonucu bulduk.

Örnek 

$$ (1+\cos(x))y' - (\sin(x))y = 2x $$

$$ y' = \frac{\sin(x)}{1+\cos(x)}y = \frac{2x}{1+\cos(x)} $$

Ent. edici faktör

$$ - \int \frac{\sin(x)}{1+\cos(x)} \ud x $$

Bu bayaðý korkutucu bir entegral gibi gözüküyor. Fakat dikkatli bakarsak
bölümün üst tarafý alt tarafýnýn türevi. Bu çok iyi, o zaman entegrasyon þu
sonucu verir:

$$ \ln (1+\cos(x)) $$

Faktör ise

$$ e^{\ln (1+\cos(x))} = 1+\cos(x) $$

Ýki tarafý faktör ile çarpalým

$$ (1+\cos(x)) y' - \sin(x)y = 2x $$

Burada ilginç bir durum var, bu sonuç örneðin ta kendisi! Buradan anlýyoruz
ki ODE daha baþtan beri parantez içinde gruplanabilir bir haldeymiþ. Yani

$$ [(1+\cos(x))y]' = 2x $$

Ýki tarafýn entegralini alalým

$$ (1+\cos(x))y = x^2 $$

$$ y = \frac{x^2+C}{1+\cos(x)} $$

Diyelim ki problem bize bir baþlangýç þartý, $y(0) = 1$ verdi. O zaman

$$ 1=\frac{c}{2}, c=2 $$

Bunu $y$ formülüne koyarsak çözümü tamamlamýþ oluruz. 

k Tane Sabit Ýçeren Lineer ODE

Bu formda $p(x)$ bir sabit olacak, bu sabitli form oldukça önemli, banka
hesaplarý vs. gibi pek çok modelde karþýmýza çýkýyor.

$$ \frac{dT}{dt} + kT = kT_e $$

Faktör? k'nýn entegrali nedir? $\int k \ud t = kt$. O zaman

$$ e^{kt} $$

Ýki tarafý bununla çarpalým. 

$$ (e^{kt} T)' = kT_e e^{kt} $$

Ýki tarafýn entegralini alalým

$$ e^{kt} T = \int kT_e(t) e^{kt} \ud t + C$$

$$ T = e^{-kt} \int kT_e(t) e^{kt} \ud t + C e^{-kt} $$

Çoðu insan, çoðunlukla mühendislik literatüründe, üstteki çözümü tanýmsýz
(indefinite) entegral halinde býrakmayý sevmez, çünkü tanýmsýz entegral
.. tanýmsýzdýr; baþka bir deyiþle tanýmsýz bir entegral bir deðil pek çok
mümkün fonksiyonlarý ayný anda temsil eder, ve bu tüm mümkün fonksiyonlarýn
arasýndaki fark sonsuz tane farklý olabilecek sabit sayýdýr (bu yüzden
zaten baþlangýç þartýný alarak somut bir $C$ deðeri bulmaya çalýþýrýz).

Bu tür literatürde eðer bir baþlangýç þartý var ise, mesela $T(0) = T_0$
gibi, bu kiþiler tanýmsýz entegrali þu þekilde tanýmlý hale getirmeyi
seviyorlar.

$$ T = e^{-kt} \int_0^t kT_e(t_1) e^{kt_1} \ud t_1 + C e^{-kt} $$

Yani alt sýnýr olarak sýfýr, üst sýnýr olarak $t$ alýnýyor. Yeni bir $t_1$
deðiþkeni koyulur, bu bir fuzuli (dummy) bir deðiþkendir, sadece yer
tutmasý için oraya konur. Bu neyi saðlar? Zaman $t=0$ olduðunda ne olduðuna
bakalým, o zaman toplamýn sol tarafý tamamen yokoluyor, geriye sadece
$C = T_0$ kalýyor, böylece hem $C$ deðerini $T_0$ olarak kullanabilmiþ
oluruz, hem de tanýmsýz entegrali tanýmlý hale getirmiþ oluruz.

Peki $t$ sonsuza giderken, yani çok zaman geçtikçe, bu sisteme ne olur?
$k > 0$ þartýný unutmayalým, sonsuza gidilirken bu sefer toplamýn sað
tarafý sýfýra gider. Çünkü eksi deðerli bir üstel deðer $k$ hep artý
kalacaðýna göre büyüdükçe $e$ deðerini sýfýra götürür. Bu sistemin
istikrarlý konum (steady state) çözümü þudur.

$$ T = e^{-kt} \int_0^t kT_e(t_1) e^{kt_1} \ud t_1 $$

Sýfýra giden kýsým ``geçici'' bölüm olarak adlandýrýlýr.

Bu bize bir þey daha söylüyor. Sonsuza giderken sýfýra giden bölümde
baþlangýç þartý vardý. Demek ki baþlangýç þartýnýn uzun zaman geçtikçen
sonra varýlacak nokta konusunda hiçbir önemi, etkisi yoktur. Nereden
baþlarsak baþlayalým, bir süre sonra ayný noktaya varýrýz.

\end{document}
