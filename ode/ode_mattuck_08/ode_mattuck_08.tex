\documentclass[12pt,fleqn]{article}\usepackage{../../common}
\begin{document}
Ders 8

Önceki dersi hatýrlarsak iki tarafýnda da $k$ olan, girdisi $\cos \omega t$
olan bir ODE üzerinde çalýþtýk. 

$$ y' + ky = k \cos \omega t $$

Çözmek için problemi kompleks dünyaya getirdik

$$ \tilde{y} + k\tilde{y} = ke^{i\omega t} $$

Sað tarafýn kompleks bir sayýnýn reel bölümü olduðunu farzettik
böylece. Bunu yaptýk çünkü üstel sayýlarýn entegralini almak kolay. Çözüm

$$
\tilde{y} = \frac{1}{1+i(w/k)} e^{i\omega t} 
\mlabel{1}
$$

ölmüþtü, yani çözüm þunun reel bölümü olacaktý:

$$
\frac{1}{\sqrt{1+(w/k)^2}} \cos(\omega t - \phi) 
\mlabel{2}
$$

Bu metotu kutupsal metot olarak nitelemiþtik. 

Kartezyen yöntemini kullansaydýk (1) formülünün hem üst hem alt
tarafýný kartezyene, $a+ib$ formuna çevirecektik. Ýlk önce altý reel yapmak
için altý ve üstü bölenin eþleniði (conjugate) ile çarpalým

$$ \frac{1-i(w/k)}{1+(w/k)^2} (\cos \omega t + i\sin \omega t)$$

Þimdi bunun reel kýsmýný alalým. Çarpýmý yaparken bir yandan da hayali sayý
kýsmý atýyoruz, iki iþlemi ayný anda yapýyoruz yani

$$ \frac{1}{1+(w/k)^2} (\cos \omega t + \frac{w}{k} \sin \omega t )$$

Böylece sonuca eriþtik. Þimdi sonucu kontrol edelim, hem kutupsal hem
kartezyen yönden gelince ayný sonuç eriþmemiz lazým. Bu sonuç (2)
ile ayný mý?

Ayný. Kontrol etmek için bir trigonometrik eþitlik (identity) kullanacaðýz,
bu tür geçiþleri yapabilmek dersimiz için çok önemli. Geçiþ genel formuyla
þöyle:

$$ a \cos \theta + b \sin \theta = C \cos(\theta - \phi)$$

$C$ ve $\phi$ reel sayýlardýr. Onlarýn hesabýnýn bir formülü var, onu
hatýrlamak yerine þu resmi hatýrlamak daha kolay olacaktýr

\includegraphics[height=2cm]{8_1.png}

Bizim örneðimiz için

\includegraphics[height=2cm]{8_2.png}

Geçiþi yapalým

$$ \frac{1}{1+(w/k)^2} \sqrt{1+(w/k)^2} \cos (\omega t - \phi) $$

$$ \phi = tan^{-1}(w/k) $$

Formülün sol kýsmýnda biraz temizleme yapmamýz mümkün

$$ \frac{1}{1+(w/k)^2} \sqrt{1+(w/k)^2} $$

$$ = \frac{1}{ \sqrt{ \bigg(1+(w/k)^2} \bigg)^2} \sqrt{1+(w/k)^2} $$

Karekökler artýk birleþebilir, içerideki terimler ayný olduðu için saðdaki
terim yokolur, kare yerine 1 gelir

$$ = \frac{1}{\sqrt{1+(w/k)^2}} $$

o zaman nihai formül

$$ \frac{1}{\sqrt{1+(w/k)^2}} \cos (\omega t - \phi) $$

ve bu formül (2) ile aynýdýr.

Trigonometrik geçiþin ispatlarý:

Ýspat 1

Vektörel olarak düþünürsek

\includegraphics[height=2cm]{8_3.png}

$<a,b>$ bir vektör olarak düþünülüyor, $\hat{u}_\theta$ ise birim vektör,
içeriði $<\cos \theta, \sin \theta>$. Nasýl birim peki? Çünkü $\cos^2\theta +
\sin^2\theta = 1$ deðerini verir.

Böylece $a \cos \theta + b \sin \theta$ formülünü iki vektörün nokta çarpýmý
(dot product) olarak görebiliriz, yani $<a,b> \cdot <\cos \theta, \sin \theta>$. 
Bu formu yazýnca noktasal çarpýmlarýn diðer tanýmýna zýplayabiliriz.

$$ < a,b > \cdot < \cos \theta, \sin \theta > $$

$$ = |< a,b >| \cdot 1 \cdot \cos(\theta - \phi) $$

$$ = |< a,b >| \cdot \cos(\theta - \phi) $$

Bu formül noktasal çarpýmýn tanýmýndan ileri geliyor, $x \cdot y =
|x|\cdot|y| \cos \beta$, $\beta$ iki vektör arasýndaki açý. $\theta - \phi$
çünkü iki vektör arasýndaki açý böyle.

\includegraphics[height=2cm]{8_4.png}

1 deðeri ile çarpýldý, çünkü birim vektörün büyüklüðü (magnitude) 1
deðerindedir. $|<a,b>| = \sqrt{a^2+b^2}$ demektir, yani resimde $C$ olarak
gösterilen þeydir. Böylece ispatý tamamlamýþ olduk. 

Ýspat 2

Bu ispat kompleks sayýlarý kullanacak. $a \cos \theta + b \sin \theta$
formülünü, reel kýsmý eþit olacak þekilde, alttaki þekilde temsil
edeceðiz. Stratejimiz o noktadan kutupsal forma atlamak, sonra onun reel
kýsmýný ilk formülümüz $a \cos \theta + b \sin \theta$ ile karþýlaþtýrmak
olacak. 

$$
(a - bi) (\cos \theta + i\sin \theta) 
\mlabel{3}
$$

Soldaki eksi iþaretinin sebebi bariz, çünkü saðdaki eksi ile çarpýlýnca
$i^2 = -1$ ile beraber artýya dönüþmesi için. Þimdi parça parça kutupsal
forma geçelim. $(a-bi)$ nasýl kutupsal forma geçer? $(a+bi)$ arasýndaki açý
$\phi$ ise, $(a-bi)$ arasýndaki açý $-\phi$ olur. Ýkinci kýsým zaten
$e^{i\theta}$'dir. Hep beraber

$$ = \sqrt{a^2+b^2}e^{-i\phi} e^{i\theta}$$

$$ = \sqrt{a^2+b^2} e^{i(\theta - \phi) }  $$

Formül (3)'ün reel kýsmýný alýrsak, $a \cos \theta + b \sin \theta$
elde ederiz. Üstteki son formülün reel kýsmý nedir? 

$$ = \sqrt{a^2+b^2}e^{i(\theta - \phi)}$$

$$ = \sqrt{a^2+b^2}\cos(\theta - \phi) + i\sin(\theta - \phi) $$

Reel kýsmý

$$ \sqrt{a^2+b^2}\cos(\theta - \phi)$$

Yani geçiþi tekrar ispatlamýþ olduk. 

Hoca kompleks sayý sisteminin kullanýlmasýna çok vurgu yapýyor. Dersi
öðretirken bir anlamda matematik alanýnýn yaþadýklarýný tekrar yaþatýyor
öðrencilere, matematikçilerin kompleks sayý sistemine alýþmasý için 300
küsur sene geçti, eðer öðrencileri üç hafta bu konuda harcarsa bu pek aðýr
bir yük sayýlmamalý.

Þimdi lineer formüllere geri döneceðiz. Daha önceki derslerde genelden
özele geçmiþtik, þimdi ters yönde gidelim. 

Temel Lineer ODE

$$1. \  y' + ky = kq_e(t) $$ 

En özel formüllerden biri olan bu formül ýsý / konsantrasyon ya da iletim /
difüzyon (conduction / diffusion) formülü idi.

Fakat fizik baþta olmak üzere pek çok alanda $k$ sabitinin sað tarafta
olmayabileceði durumlar da olacaktýr. Bunlardan da bahsetmek gerekir.

$$ 2. \ y' + ky = q(t) $$

Nihayet en genel durumlardan birinde $k$'nin sabit olmadýðý durumdur.

$$ 3. \ y' + p(t)y = q(t) $$

Bu formu tanýmsýz entegral kullanarak çözmeyi biliyoruz, dersin baþýnda
bunu iþlemiþtik zaten.

Bu ODE'leri çözerken çok önemli bir irdeleme, karar aný $k$'nin eksi mi
artý mý olduðuna baðlýdýr. Þimdiye kadar hep $k>0$ olduðunu farzettik. 

Karýþým Problemi

Hacmi $V$ olan bir içine $r$ hýzýnda sývý gelen bir kap düþünelim. $x(t)$:
kaptaki $t$ anýndaki tuz miktarý, $C_e$ gelen sývýnýn tuz konsantrasyonu. 

\includegraphics[height=2cm]{8_5.png}

$$ \frac{dx}{dt} = \textrm{ gelen tuz hýzý - giden tuz hýzý } $$

Hýz niye böyle temsil edildi? Hýzý ölçmek için iki zaman aralýðýnda yapýlan
ölçümlerin farkýný zaman aralýðýna böleriz. Üstteki formülde, sol tarafta
bu zaman aralýðý en küçük (infinitesimal) kesit olacak þekilde
hesaplanýyor, bu yüzden sol tarafta diferansiyel form var. 

$$ = r C_e - r \frac{x}{V} $$

Sivinin geliþ hýzý $r$'nin hem gidiþ hem geliþ için kullanýldýðýna dikkat
edelim, kap tamamen dolu, o yüzden gelen sývý oranýnda sývý mutlaka dýþarý
çýkmalý. 

Standart lineer form

$$ \frac{dx}{dt} + \frac{rx}{V} = rC_e(t) $$

$$ C(t) = \frac{x}{V} $$

Ýki tarafýn diferansiyelini alýrsak

$$ \frac{dC}{dt} = \frac{1}{V}\frac{dx}{dt}$$

$$ V\frac{dC}{dt} = \frac{dx}{dt}$$

Buna göre ana diferansiyel denklemdeki formül þöyle deðiþir

$$ V \frac{dC}{dt} + rC = rC_e$$

Standart forma koyalým. Bunu yaparken $r$'nin deðil, $r/V$'nin kritik öðe
olduðunu görüyoruz.

$$ \frac{dC}{dt} + \frac{r}{V} = \frac{r}{V} C_e $$

Bu formülde tuz miktarý yerine konsantrasyon baðýmlý deðiþkendir, ve
formülün bu halinin üstteki listede 1. form kategorisine girdiðini
görüyoruz. Tabii konsantrasyon derken (biraz kelimeler karýþmýþ olabilir),
iletim / difüzyon modelinden bahsediyoruz aslýnda. 

Ve iletim problemindeki akýþkanlýk sabiti $r/V$, sadece $r$ deðil. $r$
hacim / dakika, $V$ hacim, o zaman $r/V$ 1/dakika, yani
$\textrm{dakika}^{-1}$ olur.

Örnek

Diyelim ki $C_e$ sinüssel, $\cos \omega t$. Eðer $k$ çok büyük ise, $C(t)$,
$C_e(t)$'yi ne kadar yakýnda takip eder? Ýletim probleminden biliyoruz ki
eðer akýþkanlýk sabiti büyükse iç ýsý dýþ ýsýyý yakýndan takip
eder. Difüzyonda ayný durum.

Bizim problemimize bu nasýl tercüme edilir? Eðer $r$ çok büyükse (hýzlý
akýþ) o zaman içerideki konsantrasyon dýþa oldukça yakýn olur. Ya da $V$
çok küçüktür, o zaman da kap hýzla boþalacaktýr, yine ayný durum
olur. Sezgizel olarak anlamlý bir þey yani.

Bu demektir ki gecikme (lag) $\phi$ küçüktür, büyüklük (amplitude) ise 1'e
yakýndýr. 

Bu dersin geri kalanýnda 1. formülün, hatta bazen 2. formülün nerede ise
yaramadýðýna bakalým. 

2'nin iþlediði durumlar:

\includegraphics[height=2cm]{8_6.png}

$q$ kapasitorün üzerindeki yük (charge). 

$$ \frac{dq}{dt} = i $$

Dikkat, ``i'' burada kompleks sayý deðil, akým. 

Kýrchoff Kanununa göre

$$ R\frac{dq}{dt} + \frac{q}{C} = \varepsilon(t) $$

Standart forma koyarsak

$$ q' + \frac{q}{RC} = \frac{\varepsilon}{R} $$

Bu formülü 1. forma yaklaþtýrmak için $k=RC$ diye kabul edip, sonra sað
tarafý $k$'leþtirmek için $\varepsilon \cdot C$ almaya uðraþmak gibi
numaralar yapýlabilir, fakat bu oldukça doðal olmayan bir yaklaþýmdýr, yani
bu formül 1'de ziyade 2. form ile çözülür. 

Diðer bir örnek, zincirleme radyoaktif çürümesi (radioactive chain decay). 

\includegraphics[height=2cm]{8_7.png}

A ve B adýnda iki element var (bunlar periyodik tablodaki elementlerden),
ve A çürüyor, B oluyor, sonra B çürümeye devam ediyor. Basitlik için tek A
atomu tek B atomuna dönüþüyor diyelim. Çürüme nedir? Atomun parçalarýný
kaybetmektir, bu sebeple element deðiþimi olur. Elimdeki ne kadar B
olduðunu merak ediyorum mesela, þöyle modelleyebilirim 

$$ \frac{dB}{dt} = k_1 A - k_2 B$$

$$ = k_1A_0 e^{-k_1t} - k_2B $$

Ufak bir yer deðiþiminden sonra diferansiyel denklem 

$$ B' + k_2 B = k_1A_0 e^{-k_1t}$$

Fakat buna bakýnca anlýyoruz ki $A_0$ sabitinin $k$ sabitinin hiçbir
alakasý yok. Bu sebeple üstteki denklem 1. forma uygun deðil, doðal
durmuyor. Bu örnekte de 2. form doðru olandýr. 

Þimdi ufak bir kötü haber: Eðer $k < 0$ ise, þimdiye kadar gördüðümüz
terminolojideki geçici (transient), istikrarlý konum (steady-state), girdi
(input), cevap (response) sözlerinin hiçbiri geçerli olmaz. Denklemi
``çözme'' yöntemi aynýdýr, mesela

$$ \frac{dy}{dt} - ay = q(t)$$

yani $a > 0$ ve $k < -a$

O zaman çözüm olan þu formül

$$ e^{-kt} \int q(t) e^{kt} \ud t + ce^k$$

þu hale gelecektir

$$ e^{at} \int q(t) e^{-at} \ud t + ce^{at}$$

Ne oldu? $a>0$ olduðu için $ce^{at}$ sonsuzluða gider, bilahere tüm çözüm
sonsuzluða gider, $c < 0$ ise eksi sonsuzluða gider. O zaman bu kýsým
geçici deðil, çünkü sýfýra gitmiyor, ve ne olacaðý baþlangýç þartlarýna çok
baðlý. Kýyasla $k>0$ olduðu diðer durumda görmüþtük ki baþlangýç
þartlarýnýn ne olduðu hiç farketmiyordu.

Hangi bilim dallarýnda $k > 0$ ya da $k < 0$. 

Biyolojide, ekonomide çoðunlukla $k < 0$. 

Fizikte çoðunlukla $k > 0$ [Hocanýn esprisi yaþamayan þeyler varsa $k > 0$,
yaþayan þeyler varsa $k < 0$]. 

\end{document}
